# UNIST ρ_c 최적화 — 예비 측정 기록 (2026-08-19 ~ 08-20, 집 데스크톱)

> **이 디렉터리는 스윕 결과가 아니다.** 본 스윕(`run_unist_rhoc.py`)은 **한 점도
> 완주하지 않았고**, `out/`은 비어 있다. 여기 있는 것은 "스윕을 돌릴 수 있는가"를
> 판단하려고 잰 **타이밍·메시 진단**이며, 그 과정에서 부수적으로 나온 실제 물리
> 결과 3점이 함께 들어 있다.
>
> 왜 이렇게 남기는가: 이 숫자들은 **약 2시간 45분의 실제 계산**이고, 원래는 세션
> 로그에만 있었다. 재계산하면 1.4시간이 다시 든다. 판단 근거가 사라지지 않도록
> 저장소에 옮긴 것이다. 결정과 그 이유는
> `docs/sessions/2026-08-20-unist-rhoc-mesh-budget-blocker.md`에 있다.

## 환경

| | |
|---|---|
| 머신 | 집 데스크톱 (Win10 / AMD64) |
| 빌드 | 2L-FEST v28.66 build `4296313f88f7` |
| 솔버 변경 | **없음** — TASK C1(코드 동결)에 따라 읽기만 했다 |
| 평가 설정 | Run B, npts=8, 요청 노드 예산 82,000 |

## 파일

| 파일 | 내용 |
|---|---|
| `runb_measured_points.csv` | **실제로 푼 Run B 3점** (w_f=45 µm, pitch 0.90 / 1.40 / 3.00 mm). 재사용 가능 |
| `mesh_budget_scan.csv` | 노드 예산 20k / 40k / 82k → **전부 동일 메시·동일 결과** |
| `mesh_cost_map.csv` | 축소 24점 그리드 전체의 실제 노드 수 (메시 생성만, 솔브 없음) |
| `logs/01_single_point_probe.log` | 단일 평가 1점 타이밍 — 1,429.8 s |
| `logs/02_mesh_convergence.log` | 노드 예산 스캔 원본 |
| `logs/03_mesh_cost_map.log` | 그리드 노드 수 맵 원본 (`RUNS`/`GRID_*` 설정 포함) |
| `logs/04_solve_time_scaling.log` | 노드 수 대비 solve 시간 지수 측정 원본 |
| `logs/05_npts_solve_count.log` | **npts별 solve 호출 수** — npts 8→5의 절감은 1.8 %뿐 |
| `scripts/probe_npts_cost.py` | 위 카운트를 낸 프로브. 솔버를 해석적 스텁으로 바꿔 호출 수만 셈 (실계산 없음) |
| `scripts/*.py` | 위 측정을 낸 프로브 3종. 저장소·솔버를 건드리지 않고 드라이버를 import해 상수만 덮어쓴다 |

> `scripts/*.py`의 `ROOT`는 이 머신 경로(`C:\Users\A\Desktop\2L-FEST`)로 하드코딩돼
> 있다. 다른 PC에서 재현하려면 그 한 줄을 고칠 것.

## 핵심 수치 세 줄 요약

1. **1점 = 23.8분** (98,433 노드, npts=8). 축소 24점 그리드 = **17.2시간**.
2. **노드 예산은 작동하지 않는다** — 20k/40k/82k가 전부 98,433 노드, η가 소수점
   15자리까지 동일. 비용을 낮추는 손잡이가 아니다.
3. **비용은 pitch 단독 결정**, 시간은 노드 수에 **정확히 선형**(p = +1.00,
   0.0164 s/노드). 핑거 폭은 메시 비용에 영향 0.
4. **npts도 손잡이가 아니다** — `calc_iv`의 MPP 구간이 `max(npts+4, 16)`으로
   고정이라 npts 8→5는 solve 호출을 57→56회로 줄일 뿐이다(1.8 %). 노드 예산에
   이어 두 번째 가짜 손잡이. **비용 손잡이는 pitch 하나뿐이다.**

## 부수 소득 — 버리지 말 것

- **Voc 앵커 통과.** 프로브 점 Voc = 1.9179 V, 앵커 1.94 V 대비 잔차 −0.022 V로
  허용범위 ±0.05 안. **TASK §2의 J01_top 조정이 불필요하다는 뜻이다.**
- **w_f=45 슬라이스에서 η가 pitch 1.40 mm에서 내부 최대.** 경계값이 아니라 진짜
  봉우리라 A1(경계 금지) 통과. 다만 봉우리 폭이 0.42 %p로 매우 완만하다 —
  최적점 위치가 격자 해상도에 둔감하다는 근거.
