
# PyInstaller build artifacts (regenerate with build_app.command)
*.pyc
*.spec
.DS_Store
__pycache__/
_validate_*_out.txt
build/
dist/
build_venv/
solcore_xval/*.npz
solcore_xval/__pycache__/
solcore_xval/compare_iv.png
.pytest_cache/

# roadmap 러너 스모크 산출물 (scripts/run_roadmap.py docstring의 권장 명령이
# scripts/scenarios/_smoke.csv 로 쓴다). 실제 시나리오 결과는 _ 없는 이름을 쓸 것.
scripts/scenarios/_*.csv
scripts/scenarios/_*.png

# 루트 실험 산출물 중 보존 가치가 없는 것.
#   실험 결과 CSV(_*_results.csv)는 반대로 **추적한다** — 조건은
#   _experiments_README.md에 기록되어 있다.
GEDOS_Results.csv
pytest.log
_edgeopt_out.txt

# 작업 기록 — 세션마다 재생성되는 점검·테스트 산출물.
doc_check.txt
m10_result.txt
