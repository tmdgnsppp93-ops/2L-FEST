# 2L-FEST PRO — runtime dependencies
# Python >= 3.9 권장 (개발 환경: Python 3.9.6)
# 설치:  pip install -r requirements.txt

numpy>=1.24
scipy>=1.10
matplotlib>=3.7
customtkinter>=5.2

# --- 선택적 의존성 (Optional) ---
# DXF 그리드 도면 가져오기/내보내기 기능 사용 시에만 필요
ezdxf>=1.0
