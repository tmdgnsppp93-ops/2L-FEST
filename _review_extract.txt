
####################################################################################################
### DIM: geometry | findings:8 verified:5
SUMMARY: The core H-pattern construction is geometrically sound: busbars at x = W*(i+1)/(n_b+1) and fingers at y = H*(i+1)/(n_f+1) form a correctly centered, symmetric grid in consistent cm units, and the exact triangle-rectangle clipping for FEM metal fraction is robust. However, the closed-form shading_fraction() uses a constant overlap correction A_ov = n_f*n_b*w_f*w_b that systematically over-subtracts whenever busbars are short (busbar_length_frac<1), fingers are short (finger_length_frac<1 / edge_gap>0), or the pattern is shingled — in all those cases not every finger actually crosses every busbar, so reported shading is too low and Jsc/efficiency too high. Secondary issues: tapered_h widening is ignored in shading, the front pad rectangle is drawn at the wafer top while it is classified at the busbar top (inconsistent when busbar is short), and circular wafers count metal lying outside the wafer disk as shading. Unit handling (mm/cm/um) in derive_n_fingers, pitch and the metal rects is otherwise consistent and verified.

[CRITICAL|conf=high] GEO-1: shading_fraction over-subtracts finger/busbar overlap when busbar or finger is shorter than the cell  >>VERIFY[{'finding_id': 'GEO-1', 'verdict': 'partially_confirmed', 'reasoning': "The quoted code exists verbatim. L846: `A_ov = self.n_f * self.n_b * w_f_opt * w_b_opt`; L849: `return (A_f + A_b + A_p - A_ov) / self.wafer_area()`. L835-836 use the actual finger length `fg_len = self.fg_x_range[1]-self.fg_x_range[0]`; L839-840 use the actual busbar length `bb_len = self.bb_y_range[1]-self.bb_y_range[0]`.\n\nThe GEOMETRIC mechanism is real and correctly described. From compute_positions (L595-612): fingers sit at fg_y[i]=H*(i+1)/(n_f+1) spanning nearly full height; busbar y-span is bb_y_range=[bb_margin, H-bb_margin] with bb_margin=(1-busbar_length_frac)*H/2 (L610-612); finger x-span is fg_x_range trimmed by finger_length_frac/edge_gap (L603-607). A finger crosses a busbar only if fg_y[i] lies in bb_y_range AND bb_x[j] lies in fg_x_range. When busbar_length_frac<1 or finger_length_frac<1/edge_gap>0, true crossings < n_f*n_b, yet the code always subtracts n_f*n_b. I replicated the exact geometry numerically: for the reviewer's case (156mm, 100F/5BB, busbar_length_frac=0.5) exactly 50 of 100 fingers fall inside bb_y_range=[3.9,11.7], true crossings=250 but code subtracts 500. The sign/direction is exactly as claimed: too much overlap subtracted -> shading UNDER-estimated -> Jsc/efficiency OVER-estimated. It does feed calc_iv via shade_frac/P_shade (L6074-6076) and the _gen_s generation-scaling ratio (L3280-3286).\n\nWHERE THE CLAIM FAILS: severity. The over-subtracted ('phantom') area is only (#phantom_crossings)*(w_f*w_b) — tiny 50um x 50um squares. For the reviewer's own 100F/5BB/blf=0.5 example the shading is under-estimated by just 0.0026 percentage points (3.2801% vs 3.2827%), i.e. 0.08% relative. Even a pathological case (150F/12BB, 200um widths, blf=0.3, flf=0.5) yields only ~0.25 %abs / ~2.5% relative. The claim that 'reported shading drops by the area of 50 phantom overlaps' is literally true but those overlaps total a negligible area (250 x (50e-4)^2 = 6.25e-4 cm^2 out of 243 cm^2). This cannot materially move Jsc/efficiency and is nowhere near 'critical'.", 'corrected_severity': 'minor', 'corrected_location': 'CellGeometry.shading_fraction L844-846 (A_ov = self.n_f*self.n_b*w_f_opt*w_b_opt), with consumers at L3280-3286 (_gen_s) and L6074-6076 (P_shade)'}] sev->None
   LOC: CellGeometry.shading_fraction L828-849 (A_ov L846)

[MEDIUM|conf=high] GEO-2: Shingled/fork pattern: shading subtracts a finger-busbar overlap that does not exist  >>VERIFY[{'finding_id': 'GEO-2', 'verdict': 'confirmed', 'reasoning': 'I read the actual code and the reviewer\'s quotes are exact. In _compute_shingled: L669 `bb_inset = self.w_b / 2 + 1e-4` puts the single busbar center at x = w_b/2 + 1e-4, so its x-extent is [1e-4, w_b + 1e-4]. L677 `fg_start = self.w_b + 1e-4` makes fingers start exactly at the busbar\'s right edge (w_b + 1e-4). The busbar region [1e-4, w_b+1e-4] and finger regions [w_b+1e-4, ...] are disjoint in x — the fingers abut the busbar with zero shared area. So in shingled/fork mode the true finger-busbar overlap is essentially zero.\\n\\nYet shading_fraction (L846) computes `A_ov = self.n_f * self.n_b * w_f_opt * w_b_opt` and subtracts it at L849 `(A_f + A_b + A_p - A_ov)/wafer_area()`. The inline comment at L845 even asserts "busbar is at bb_x -- always overlaps," which is true for h_pattern (where busbars sit mid-span and every finger genuinely crosses every busbar, making A_ov = n_f*n_b*w_f*w_b correct) but false for shingled. The same formula is reused for both modes without accounting for the abutting geometry, so shingled shading is under-estimated by exactly n_f*w_f*w_b/wafer_area.\\n\\nThis is NOT just a cosmetic display value. shading_fraction feeds physically meaningful outputs: (1) L3280-3286 scales photogeneration current illum_frac = base*(1-shade_case)/(1-shade_design); an under-estimated shade inflates generated current; (2) L6074-6076 computes shading power loss P_shade = shade_frac * Jmpp * Vmpp, which is under-estimated. No compensating term exists.\\n\\nI reproduced the magnitude for the reviewer\'s example (100 fingers, 1 mm busbar = 0.10 cm, 40 um fingers = 0.004 cm, 15.6 cm cell): A_ov = 0.04 cm^2, A_ov/wafer = 1.64e-4 = 0.0164% absolute — matching the reviewer\'s ~0.016%. The bias is small per design but consistently optimistic and grows linearly with finger count and busbar width, and shingled/fork is an explicitly supported geometry (L503, L662). Severity medium is appropriate: real but small first-order error on a supported-but-not-default pattern.', 'corrected_severity': 'medium', 'corrected_location': 'GridDesign._compute_shingled L669 (bb_inset) and L677 (fg_start); CellGeometry.shading_fraction L844-849 (A_ov reused from h_pattern). Downstream impact: FESTSolver._build L3280-3286 (generation scaling) and losses L6074-6076 (shading power loss).'}] sev->None
   LOC: GridDesign._compute_shingled L661-722 (fg_start L677) + shading_fraction L846

[MEDIUM|conf=high] GEO-3: Front pad drawn at wafer top in metal_rects but classified at busbar top — inconsistent when busbar is short  >>VERIFY[{'finding_id': 'GEO-3', 'verdict': 'confirmed', 'reasoning': 'All quoted code exists verbatim and the physics reasoning holds.\n\n(1) metal_rects_front L901-902: `if self.pad > 0: rects.append((self.pad_cx - self.pad/2, self.H - self.pad, self.pad, self.pad))` — pad rect anchored to y in [H - pad, H], i.e. the WAFER TOP. This is the exact list consumed by compute_metal_frac L1772 (`rects = geo.metal_rects_front()`) which feeds self.metal_frac (L2971), used everywhere in the solver for illumination blocking / generation (e.g. L3252 `self._Gc[self.ism] = ... self.metal_frac[...]`).\n\n(2) compute_positions L650-651: `# pad centered on BB top (inside BB), not wafer top` / `pad_cy = bb_y_range[1] - self.pad/2 if self.pad > 0 ...`. From L610-612, bb_y_range = (bb_margin, H - bb_margin) with bb_margin = (H - busbar_length_frac*H)/2, so bb_y_range[1] = H - bb_margin < H whenever busbar_length_frac < 1. Thus pad_cy (busbar top) != H - pad/2 (wafer top) for a short busbar.\n\n(3) classify_nodes L1655-1658: `is_pad = ((np.abs(x - geo.pad_cx) <= geo.pad/2+1e-6) & (np.abs(y - geo.pad_cy) <= geo.pad/2+1e-6))` — box centered at (pad_cx, pad_cy) = busbar top. The comment at L1656 even reads "previously anchored to wafer top," confirming the location was deliberately moved for classification but NOT for metal_rects. This is_pad becomes self.isp (L2922) and self.pidx = np.where(isp)[0] (L2928), which are the front probe / Dirichlet BC nodes (L3568 `pmk = [self.mmap[gi] for gi in self.pidx ...]`).\n\nSo with pad>0 and busbar_length_frac<1 there are genuinely two physical pad locations: the metal-area/illumination pad (wafer top) and the probe/BC node (busbar top). I found the inconsistency is in fact three-way and slightly worse than stated: both mesh seeders also anchor pad nodes to the wafer top — generate_mesh_legacy L1432-1433 (`for yi in np.linspace(H - geo.pad, H, 8)`) and the modern generate_mesh L1566-1571, where `pcx, pcy = geo.pad_cx, geo.pad_cy` is unpacked but pcy is never used and the y-loop hardcodes `_nested_linspace(H - geo.pad, H, ...)`. So mesh refinement is seeded at the wafer top while classify_nodes searches for is_pad nodes near the busbar top; for a sufficiently short busbar the probe box may capture few/no refined nodes and the metal patch sits where no current is extracted, distorting the local Rs path and shading bookkeeping. The DXF/poly path (classify_nodes_poly L2370, generate_mesh_poly) uses terminals directly and is not affected.\n\nDormancy confirmed: pad_size defaults to 0.0 (L489, L733), and busbar_length_frac defaults to 1.0 (L488); when busbar_length_frac==1, bb_y_range[1]==H and pad_cy==H-pad/2 matches the wafer-top rect, so no inconsistency. The bug requires pad>0 AND a short busbar simultaneously, both off-default. Severity medium (dormant by default, not silently wrong on the default path) is appropriate.', 'corrected_severity': 'medium', 'corrected_location': 'metal_rects_front L901-902 (and mesh seeders generate_mesh_legacy L1432-1433, generate_mesh L1566-1571 which also anchor pad to wafer top) vs compute_positions L650-651 vs classify_nodes L1655-1658 (is_pad -> self.isp/self.pidx at FESTSolver L2922-2928, probe BC at L3568)'}] sev->None
   LOC: CellGeometry.metal_rects_front L901-902 vs compute_positions L650-651 vs classify_nodes L1655-1658

[MEDIUM|conf=high] GEO-4: tapered_h finger widening is ignored by shading_fraction (and by the 1D resistance band)  >>VERIFY[{'finding_id': 'GEO-4', 'verdict': 'partially_confirmed', 'reasoning': "Quoted code is exact. shading_fraction L834-836: `fg_len = self.fg_x_range[1]-self.fg_x_range[0]; A_f = self.n_f * w_f_opt * fg_len` — base finger width only, no taper term. metal_rects_front L864-886: `taper_w = self.w_f * self.front.taper_factor` and `rects.append((t_left, fy - taper_w/2, t_right-t_left, taper_w))` — taper-widened rects. compute_metal_frac L1772 consumes metal_rects_front() so the FEM metal_frac IS taper-aware, while shading_fraction is NOT. The two metal-area models genuinely disagree for pattern_style=='tapered_h'. The parenthetical 1D-resistance claim also holds: assemble_K_met_1d L2095-2102 uses `w_f_eff = geo.w_f` and band half-width `geo.w_f/2.0`, so the wider tapered cross-section (lower R near busbars) is not modeled. So the CORE inconsistency the reviewer flags is REAL and exists exactly where cited.\\n\\nBUT the stated IMPACT ('reported shading too LOW so Jsc/efficiency are over-estimated, and the optimizer sees an inconsistent shading vs FEM model') is largely wrong. Photo-generation does NOT flow through shading_fraction: L2972 sets `illum_frac = 1.0 - metal_frac` from compute_metal_frac (taper-aware), and Jph = illum_frac*Jph_top (e.g. L6155, L5191). The only use of shading_fraction in the solve path is the global scale _gen_s = (1-sh_case)/(1-sh_geo) at L3280-3283 — a RATIO of two base-width shadings, so the missing taper term cancels and cannot bias generation. Hence the FEM-computed Jsc/Eff/Pmpp are NOT over-estimated by this omission. The optimizer-comparison lines the reviewer cites (L9944-9945, L10893-10894) only fill a display 'Shading [%]' table row via _draw_table (L9955-9956); the ranked Jsc/Eff/Pmpp in the same table come from the FEM IV solve (iv_b/iv_a), not from shading_fraction. So the optimizer does not rank on the inconsistent shading number. Also note: for DXF-imported geometry attach_dxf_to_geometry L2346-2363 rebinds shading_fraction to a rasterized rect-union that DOES match the drawn metal — so the discrepancy is confined to parametric tapered_h designs.\\n\\nNet: a genuine consistency defect that under-reports the displayed 'Shading: %' number (L999, 6679, 7803) and under-counts the analytical P_shade loss-breakdown term (L6074-6076) for tapered designs, but NOT a Jsc/efficiency over-estimation and NOT an optimizer-ranking bug as claimed.", 'corrected_severity': 'minor', 'corrected_location': 'CellGeometry.shading_fraction L834-836 (base-width A_f) vs metal_rects_front L864-886 (taper_w); FEM consumer compute_metal_frac L1772; 1D band assemble_K_met_1d L2095-2102; affected reporting/loss uses: P_shade L6074-6076 and displayed Shading% at L999/6679/7803/9944-9945. Note generation path illum_frac=1-metal_frac at L2972 is taper-aware, and DXF override shading at L2346-2363 is consistent.'}] sev->None
   LOC: CellGeometry.shading_fraction L834-836; metal_rects_front L860-891 (taper_w) 

[MEDIUM|conf=medium] GEO-5: Circular wafer: metal extending outside the disk is counted as shading (numerator/denominator mismatch)  >>VERIFY[{'finding_id': 'GEO-5', 'verdict': 'confirmed', 'reasoning': 'All claims verified against the actual code.\n\n(a) Quoted code exists verbatim. wafer_area() L794-796: `if self.wafer_shape == "circular": r = min(self.W, self.H) / 2.0; return np.pi * r * r`. shading_fraction() L848-849: `# divide by ACTUAL wafer area ...; return (A_f + A_b + A_p - A_ov) / self.wafer_area()`.\n\n(b) The numerator/denominator mismatch is real. In shading_fraction (L835-846): fg_len = fg_x_range[1]-fg_x_range[0], bb_len = bb_y_range[1]-bb_y_range[0], and A_f = n_f*w_f*fg_len. These ranges come from GridDesign.compute_positions (called in __init__ L765), which computes them purely from the square W/H with NO wafer_shape awareness: fg_x_range = (fg_margin, W - fg_margin) (L607), bb_y_range = (bb_margin, H - bb_margin) (L612), fg_y = [H*(i+1)/(n_f+1) ...] (L599). So fingers/busbars span the full square box and fingers are distributed across the full height H, including rows near y=0/y=H whose tips lie outside the inscribed disk. Yet the denominator for circular is pi*r^2 (~0.785*W*H). The metal pattern is never clipped to wafer_outline_xy_mm (L804) — confirmed: metal_rects_front (L851-903) uses the square ranges; compute_metal_frac (L1767) and the FEM mesh (add() clips points to [0,W]x[0,H], L1480) all operate on the square box; the DXF override _shading (L2346-2359) rasterizes over linspace(0,W)/linspace(0,H) then divides cov.mean()*(W*H) by wafer_area() — the identical mismatch.\n\n(c) Impact confirmed. shading_fraction is not purely cosmetic: it feeds P_shade in the losses() method (L6073-6076: shade_frac = self.geo.shading_fraction(wf, wb_eff); P_shade = shade_frac * Jmpp * Vmpp), so it distorts the reported shading-loss power / analytical breakdown. I independently reproduced the reviewer\'s numbers with a representative full-width-finger case: A_f/A_square = 2.564% vs A_f/A_circ = 3.265% (ratio = 4/pi = 1.273), i.e. ~0.70% absolute over-counting — matching the claimed 2.56%/3.27%/~0.7%.\n\nThis is a genuine consistency bug: metal area measured over the square span is divided by the smaller disk area for circular (and, via the 2*c^2/2 chamfer subtraction in wafer_area L800-801, more weakly pseudo_square) wafers, over-counting shading — opposite-sign to GEO-1.', 'corrected_severity': 'medium', 'corrected_location': 'wafer_area L791-802 (circular branch L794-796) + shading_fraction L835-849; metal ranges set in GridDesign.compute_positions L599/L607/L612 (square-based, no wafer_shape); DXF override _shading L2346-2359; consumed by losses() P_shade L6073-6076'}] sev->None
   LOC: wafer_area L791-802 (circular) + shading_fraction L848-849; metal_rects use square fg_x_range

[MINOR|conf=high] GEO-6: derive_n_fingers rounds count, so realized finger pitch deviates from requested spacing on non-divisible cells
   LOC: GridDesign.derive_n_fingers L542-560, get_finger_pitch_mm L562-571

[MINOR|conf=high] GEO-7: Dead/duplicated code after return in _compute_rear_metal_frac (maintainability hazard)
   LOC: _compute_rear_metal_frac L1797-1849 (unreachable block L1828-1849)

[TRUSTWORTHY|conf=high] GEO-8: Correct centered, unit-consistent H-pattern construction and exact FEM metal clipping
   LOC: compute_positions L595-612, compute_metal_frac L1767-1794, _tri_rect_area L1758-1764

####################################################################################################
### DIM: mesh | findings:8 verified:3
SUMMARY: The mesh engine is structurally sound where it matters most: it explicitly inserts nodes on every metal edge (fy +/- w_f/2, bx +/- w_b/2) at every refinement level, classification bands are consistent with those edge nodes, and the switch from a 2D Galerkin sheet to a 1D series-resistor metal model (assemble_K_met_1d) makes the dominant finger/busbar resistance genuinely mesh-convergent. However, the "refinement near features" is largely nominal: the transition zone is mis-scaled (first transition node sits 500 um beyond a finger edge while the background grid is only 35-70 um), so it adds no useful near-edge density; the background grid itself is dyadically non-uniform (2x spacing variation) rather than refined toward metal; and the FEM domain is always the full WxH rectangle even for pseudo_square/circular wafers, so loss/generation are integrated over non-existent corner area while PCE divides by a smaller wafer_area. There is also confirmed dead code in _compute_rear_metal_frac and a hidden side effect in shingled geometry. None of these break connectivity, but several can bias FF/efficiency under specific geometries and undermine the claimed adaptivity.

[MEDIUM|conf=high] MESH-1: Transition-zone refinement is mis-scaled: nodes land 500-2000 um beyond finger, far coarser than the background grid (refinement is a no-op near the edge)  >>VERIFY[{'finding_id': 'MESH-1', 'verdict': 'refuted', 'reasoning': "The quoted code exists verbatim and the arithmetic is correct: at L1456 transition_mm=2.0; L1487 trans_cm = transition_mm/10.0 = 0.2 cm; L1494 transition_y_segments = min(8, 2<<perp_power) = 4 for Med (perp_power=1); L1524-1526 _nested_linspace(0, 0.2, 4)[1:] yields offsets of 500/1000/1500/2000 um beyond the finger edge (verified numerically). Geometry IS in cm (w_finger=50e-4 cm = 50 um, GridDesign.__init__ L487; CellGeometry default W=H=0.90 cm, L732). So the transition loop alone does add coarse 500-2000 um nodes.\n\nBUT the reviewer's core factual premise is false. The transition loop is NOT the only near-edge refinement. The background grid loop at L1483-1485 / L1484 (_nested_linspace(0, H, y_segments) with y_segments = axis_segments = 210 for Med) already populates the perpendicular direction. I built the default geometry and ran the actual generate_mesh, then sliced an x-slab through a finger and measured the real node profile: above the finger top edge (offset 0) the nodes sit at +33.6, +68.8, +103.9, +139, +174, +209, ... um with a UNIFORM 35.2 um spacing throughout the 0-500 um band — i.e. AS FINE OR FINER than the background and ~14x finer than the 500 um floor the reviewer claims. The explicit edge nodes (fy-w_f/2, fy, fy+w_f/2 at L1516) plus the across_y loop (L1518-1521, across_y_segments=4) resolve the finger interior at ~12.5 um. So there is NO empty 0-500 um band and NO 'abrupt jump from the explicit edge node straight into the background grid' — the claim is directly contradicted by the executed mesh.\n\nThe transition loop is at worst partially redundant on the small default cell. On large cells it actively helps: for a 15.6 cm, 40-finger cell the background spacing is ~609 um, and the measured mesh shows the transition nodes at +500/+1000/+1500/+2000 um grading the inter-finger gap (next finger at +3775 um) finer than the background — the opposite of a no-op.\n\nThe IMPACT claim (near-edge under-resolution -> under-estimated distributed Rs -> over-estimated FF/PCE) rests entirely on the false 500 um near-edge premise and collapses. I also refuted the 'higher mesh level does not fix it' sub-claim: near-edge resolution is set by axis_segments (Low/Med/High/Max = 165/210/280/360 -> measured background gap 70.3/35.2/35.2/17.6 um), so raising the mesh level demonstrably refines the near-edge band. No unit error, no missing refinement, no systematic physics bias.", 'corrected_severity': 'not_a_bug', 'corrected_location': 'generate_mesh L1483-1540 (background grid loop L1483-1485 plus transition loops L1522-1540); the reviewer omitted the background grid (_nested_linspace(0,H,axis_segments)) which sets the actual near-edge resolution to ~35 um for Med, not 500 um.'}] sev->None
   LOC: generate_mesh L1487-1540 (transition loops); transition_mm default = 2.0 (L1456); _nested_linspace L1222-1248

[MEDIUM|conf=high] MESH-2: FEM mesh always tessellates the full WxH rectangle; pseudo_square/circular wafer shape only changes the area denominator, so loss/generation are integrated over non-existent corner/edge area  >>VERIFY[{'finding_id': 'MESH-2', 'verdict': 'partially_confirmed', 'reasoning': 'The factual code observations are accurate, but the headline IMPACT (double-distorted PCE) is wrong.\n\nVERIFIED TRUE (mechanism):\n- L1479-1481: `def add(x,y): if -1e-12 <= x <= W+1e-12 and -1e-12 <= y <= H+1e-12: pts.add(...)` — clamps only to the bounding rectangle, no `wafer_outline_xy_mm()` check. Exists exactly as quoted.\n- L791-802: `wafer_area()` returns `np.pi*r*r` (circular) / `max(0.5*W*H, W*H - 2*c*c)` (pseudo_square). Exists.\n- L2958-2960: `self.areas` = raw triangle areas of the Delaunay triangulation. `Delaunay(points)` always tessellates the convex hull = full [0,W]x[0,H] rectangle.\n- `classify_nodes` (L1633-1721) only classifies metal (finger/busbar/pad); there is NO point-in-polygon masking of nodes or areas to the wafer outline. `generate_mesh_poly`/`classify_nodes_poly` (L2405/L2370) are DXF-only and also clamp to [0,W]x[0,H] with no outline mask.\n- L1886-1888: nodal areas `na += A/3` accumulated over the full hull. L5370/5384 `cell_current` returns `np.sum(Jt*self._na)/self.geo.area*1000`, integrating generation over ALL nodes incl. non-existent corners; `illum_frac` (L2972) accounts only for metal shading, not the outline. So generation and the resistive solve DO cover the full square for pseudo_square/circular wafers. This half of the claim holds.\n\nREFUTED (impact / double-distortion):\n- L744: `self.area = cell_w*cell_h` is explicitly the FULL bounding rectangle ("bounding rectangle area (legacy)"), NOT wafer_area(). L5539 confirms `sum(self._na) == self.geo.area` = W*H.\n- `cell_current` returns a DENSITY: `sum(J*na)/geo.area*1000`. For uniform illumination this is the area-weighted mean J; the W*H factor cancels. Jsc per cm² is NOT inflated by "too much area" as claimed.\n- L5953: `Eff = Pmpp/100*100` (Pmpp in mW/cm², AM1.5G=100 mW/cm²). Efficiency is per-cm² and is NEVER divided by wafer_area(). The ONLY engine call to wafer_area() is L8252, a GUI status-bar string ("area = X cm²"). losses() uses geo.area=W*H (L6082), not wafer_area(). So there is NO division by the smaller wafer_area in the PCE path → the claimed "double-distortion" does not occur.\n- wafer_area() also feeds shading_fraction() (L849), but in _build the only use is the case/design ratio `(1-sh_case)/(1-sh_geo)` (L3280-3283) where wafer_area() cancels between numerator and denominator → zero net effect on absolute PCE.\n\nNet: the only real residual issue is that corner/edge area participates in the lateral resistive transport for non-square shapes, slightly perturbing the spatial loss distribution — a second-order modeling inconsistency, not the PCE bookkeeping bug described. Square wafers (default) are entirely unaffected. The quantitative impact (~21% for circular, "double-distorting PCE") is not supported by the code.', 'corrected_severity': 'minor', 'corrected_location': 'generate_mesh add() clamp L1479-1481 and Delaunay hull L1574; absence of outline masking in classify_nodes L1633; current density cell_current L5370/L5384 with self.geo.area=W*H (L744, L5539); Eff path L5953 (no wafer_area division); only engine wafer_area() use is GUI status L8252'}] sev->None
   LOC: generate_mesh add() clamp L1479-1481; wafer_area() L791-802; shading_fraction L848-849; FESTSolver area integration via self.areas L2958-2960

[MEDIUM|conf=high] MESH-3: Background grid is dyadically NON-uniform (2x spacing variation), not refined toward metal; coarse half-cells can straddle thin fingers  >>VERIFY[{'finding_id': 'MESH-3', 'verdict': 'partially_confirmed', 'reasoning': "VERIFIED FACTS (true part of claim):\n(a) The quoted code exists verbatim. Lines 1483-1485: `for xi in _nested_linspace(0, W, x_segments): for yi in _nested_linspace(0, H, y_segments): add(xi, yi)`. _MESH_LEVELS (line 1212) gives Med axis_segments=210, Low=165 (line 1211) — matching the reviewer's tested values.\n(b) `_nested_linspace` (L1222-1248) is genuinely a dyadic point set. I ran it: for seg=210 it returns 211 points with EXACTLY two spacing values (585.94um and 1171.88um on a 15cm span), ratio exactly 2.0000; for seg=165, ratio 2.0000; for 280 and 360 also 2.0000. So 'dyadically NON-uniform, 2x spacing variation' is factually correct. The spacings are interleaved across the whole domain by binary-fraction ordering, not graded toward edges. The background fill at L1483-1485 uses this global grid with no dependence on metal position — so 'background grid density is independent of distance to metal' is TRUE for that layer.\n\nOVERSTATED / REFUTED parts (severity-driving impact):\nThe reviewer treats the background fill as the whole story and concludes 'a single emitter triangle spans both the near-finger and bulk region' and 'refinement is only nominal.' This ignores generate_mesh's explicit metal-aware refinement that runs AFTER the background fill:\n- add_grid_features (L1507-1556) inserts, for EVERY finger, nodes at the exact edges and center `fy - w_f/2, fy, fy + w_f/2` (L1516), an across-finger layer via across_y_segments (L1518-1521), and a decaying transition zone `_nested_linspace(0, trans_cm, transition_y_segments)` stepping away from the finger edge (L1522-1526). Busbars get the same (L1528-1540), plus finger/busbar intersection blocks (L1542-1550) and terminal rings (L1497-1505).\n- Because the exact finger edges fy±w_f/2 are guaranteed mesh vertices, and classify_nodes (L1647) keys metal classification off those same exact edges, the Delaunay triangulation is forced to align to finger boundaries; a triangle cannot freely 'straddle' a finger whose two edges are mandatory vertex lines, and the transition nodes prevent one triangle from spanning edge-to-bulk. So the mesh as a whole IS locally refined near metal; only the coarse background filler is uniform-dyadic.\n- The 'FF/efficiency wander (non-monotone convergence)' impact is asserted, not demonstrated; coarser levels remain strict subsets of finer ones (prolongation-valid, per the docstring and confirmed by the nested construction), which actually favors monotone, not chaotic, behavior. assemble_K (L1855) integrates per-element with proper areas, so 2x cell-size variation produces a graded mesh but not an ill-posed one.\n\nNet: the descriptive/numeric claim (dyadic, ratio 2.0, background not metal-refined) is CONFIRMED; the consequence framing (nodes never concentrate near gradients, single triangle spans both regions, FF wanders) is OVERSTATED because explicit finger/busbar/transition refinement exists. This is a real but minor meshing-quality observation, not a medium-severity correctness defect.", 'corrected_severity': 'minor', 'corrected_location': '_nested_linspace L1222-1248 (dyadic 2x spacing, verified ratio 2.0); background fill generate_mesh L1483-1485; BUT mitigating metal-aware refinement at add_grid_features L1507-1556 (finger edges L1516, transition L1522-1526) and classify_nodes L1647 must be accounted for'}] sev->None
   LOC: _nested_linspace L1222-1248; background fill in generate_mesh L1483-1485

[MINOR|conf=high] MESH-4: Dead/unreachable duplicate block in _compute_rear_metal_frac after an unconditional return
   LOC: _compute_rear_metal_frac L1797-1849 (return at L1826, dead code L1828-1849)

[MINOR|conf=medium] MESH-5: Shingled geometry mutates n_b as a side effect of compute_positions, risking mesh/classification using a stale busbar count
   LOC: _compute_shingled L667 (self.n_b = 1); GridDesign.__init__ L528; CellGeometry copies aliases at L776

[MINOR|conf=medium] MESH-6: across-finger fill caps at 4 nodes over the finger width regardless of level, so a 50 um finger interior never refines
   LOC: generate_mesh across fills L1491-1492, L1518-1521, L1533-1535

[TRUSTWORTHY|conf=high] MESH-7: 1D metal model + explicit edge nodes make finger/busbar series resistance and node classification genuinely mesh-convergent and index-consistent (correct)
   LOC: assemble_K_met_1d L1994-2110; generate_mesh edge insertion L1514-1531; classify_nodes bands L1646-1653; FESTSolver index maps L2926-2952

[TRUSTWORTHY|conf=high] MESH-8: compute_metal_frac uses exact Sutherland-Hodgman triangle-rectangle clipping with area-weighted nodal distribution (correct), not a sampling approximation
   LOC: _clip_poly L1727-1744; _poly_area L1747-1755; _tri_rect_area L1758-1764; compute_metal_frac L1767-1794

####################################################################################################
### DIM: assembly | findings:10 verified:5
SUMMARY: The core Galerkin sheet-conductance assembly (assemble_K) and the 1D front-metal conductor model (assemble_K_met_1d) are dimensionally consistent in the project's cm / Ohm*cm / Ohm-per-square unit system, and both produce symmetric, row-sum-zero (graph-Laplacian) operators that conserve current — current injection It=Jt*na and the metal KCL Km@Vm are wired correctly. The diagonal-absorption trick in assemble_K_met is mathematically clean (it exactly restores metal-only row-sum-zero and keeps the metal-metal submatrix symmetric PSD). The most material concerns are: (1) the REAR metal grid still uses the 2D Galerkin sheet model (assemble_K_met) that the authors themselves abandoned on the front because it is mesh-dependent for thin lines, so bifacial/patterned-rear results inherit that non-convergence; (2) several reporting/diagnostic units are mismatched (_analytical_loss labeled mW/cm2 but returns a fractional quantity; finger/busbar loss split misattributes intersection edges); and (3) the rear contact conductance drops the metal_frac weighting the front uses, biasing rear contact loss low. None of the front-path efficiency physics shows a hard sign/index bug.

[MEDIUM|conf=high] ASM-1: Rear metal grid uses the mesh-dependent 2D Galerkin sheet model the front explicitly abandoned  >>VERIFY[{'finding_id': 'ASM-1', 'verdict': 'confirmed', 'reasoning': 'Verified all factual claims against the actual code.\n\n(a) Quoted code exists exactly. L3193-3199 builds self._Krm via assemble_K_met for the rear metal grid in bifacial/patterned mode:\n  self._Krm = assemble_K_met(self.pts, self.simp, self.isrm, rm, hf, self.geo.rear.w_f if self.geo.rear else wf, cf, np.zeros(self.N, dtype=bool), self.isrm, self.areas, self.b, self.c, self.geo.rear.w_b if self.geo.rear else 0.05)\nThis sits inside the `if self.geo.rear_mode in (\'bifacial\',\'patterned\') and self.isrm is not None:` branch (L3181).\n\n(b) The front contrast is real. L3246-3248 builds the front metal via assemble_K_met_1d (the 1D model), with the comment block L3220-3225 explicitly stating the front was "switched from 2D Galerkin sheet (assemble_K_met) to a 1D conductor model" because "the 2D version produced mesh-dependent finger conductance via sliver triangles when w_f << node spacing, causing efficiency to diverge under refinement."\n\n(c) assemble_K_met (L1893-1991) is indeed the 2D Galerkin element assembler: it selects elements with n_metal>=2 (L1920), builds coeff = 1/(4*A*Rs_a) using shape-function gradients bv,cv (L1939, L1947) — the classic FEM sheet model whose conductance depends on element shape, hence mesh-dependent for slivers. Its own successor\'s docstring (L1997-2016) documents exactly this divergence: "Sliver elements have ill-conditioned shape-function gradients, so the longitudinal finger conductance becomes mesh-dependent... efficiency diverges, FF drifts." Verified spread 0.59%(2D)->0.08%(1D).\n\n(d) The rear metal genuinely has thin finger lines: metal_rects_rear (L905-917) emits rear fingers of width rear.w_f and busbars of rear.w_b, structurally analogous to the front. So the same w_line << node-spacing sliver problem applies.\n\n(e) Not dead code / no compensation: self._Krm feeds the rear-metal residual in the bifacial solvers — e.g. L4929 F[oVrm...] = (self._Krm @ Vrm_full)[self.rear_midx] + Gc_rear*(...), and L5217 likewise. So _Krm directly sets the rear metal lateral conductance and thus rear series loss / FF.\n\nNo unit conversion or compensating mechanism elsewhere mitigates this; the rear metal lateral conductance is computed by the very model the authors deemed non-convergent. The reviewer\'s scoping (only bifacial/patterned-rear runs affected; front-only/full-area unaffected via the else branch L3212-3218 setting _Krm=None) is correct.\n\nSeverity medium is appropriate: it is a genuine convergence/correctness defect, but scoped to bifacial/patterned-rear runs, and rear metal series R is a secondary loss term (rear collection through a TCO sheet) rather than a primary one, so the magnitude on overall efficiency/FF is bounded and depends on mesh refinement and rear line width.', 'corrected_severity': 'medium', 'corrected_location': '_build: rear metal assembly at L3193-3199 (call inside bifacial/patterned branch L3181); assemble_K_met definition L1893-1991; front 1D contrast at L3246-3248 with rationale comment L3220-3225 and successor docstring L1997-2016; _Krm consumed in rear residual at L4929 and L5217.'}] sev->None
   LOC: _build L3190-3199 (call), assemble_K_met L1893-1992; contrast front 1D at L3246-3248

[MEDIUM|conf=high] ASM-2: Rear contact conductance omits the metal_frac weighting the front uses (binary isrm)  >>VERIFY[{'finding_id': 'ASM-2', 'verdict': 'confirmed', 'reasoning': 'The quoted code exists exactly as cited. Rear (L3204-3205): `self._Gc_rear = np.zeros(self.N); self._Gc_rear[self.isrm] = self._na[self.isrm] / rc` — binary mask `isrm`, NO metal_frac factor. Front (L3251-3252): `self._Gc = np.zeros(self.N); self._Gc[self.ism] = self._na[self.ism] * self.metal_frac[self.ism] / rc` — area-weighted by continuous `metal_frac`. The asymmetry is real.\n\nCrucially, a continuous rear metal fraction DOES exist and is computed/stored: L2985-2987 `self.rear_metal_frac = _compute_rear_metal_frac(...)` (the same area-weighting algorithm as front `compute_metal_frac`, see L1818 `min(total_inter/areas, 1.0)` and node distribution L1820-1826). So the rear could have mirrored the front exactly but does not — `rear_metal_frac` is used only for `rear_illum_frac` (L2987), never for `_Gc_rear`. The author\'s own comment at L3201-3203 reads "Weight by metal_frac at rear nodes. // Approximation: use binary isrm" — explicitly acknowledging the intended weighting was dropped.\n\nPhysics/direction check: `compute_metal_frac` and `_compute_rear_metal_frac` both clamp element fraction to <=1 (L1818/L1839), and node values are area-weighted averages, so `metal_frac<=1` and `rear_metal_frac<=1`. Using binary 1.0 instead of `rear_metal_frac<=1` makes `_Gc_rear` systematically >= the front-equivalent conductance under identical rc/na -> rear contact resistance underestimated, rear coupling too stiff. The reviewer\'s sign and mechanism are correct.\n\nNo compensation found: `rc` at L3205 is the same front `rc` (clamped only for div-by-zero at L3142); there is no rear-specific rc rescale. `_Gc_rear` feeds the rear emitter<->rear-metal KCL coupling in every bifacial solver path (L3381, L4322/4326, L4375/4385/4390, L4568/4613/4659/4703, L4927-4930, L5214-5218), so the bias propagates directly into the rear contact-loss / FF in bifacial mode. No unit error masks it (na has units of area/Rs-consistent siemens as on the front; identical treatment both sides except the missing dimensionless metal_frac).\n\nSeverity: medium is appropriate — it is a genuine, author-acknowledged front/rear modeling inconsistency that biases the bifacial rear contribution, but it is second-order: it only departs from 1 at partial-coverage (edge / coarse-tributary) nodes and only in bifacial/patterned rear mode; nodes fully under wide rear metal have rear_metal_frac~=1 where the two agree. Not a crash or gross error, but a real systematic bias. Keep at medium.', 'corrected_severity': 'medium', 'corrected_location': '_build: rear L3204-3205 vs front L3251-3252; supporting context: rear_metal_frac computed but unused at L2985-2987; intent-vs-approximation comment L3201-3203'}] sev->None
   LOC: _build L3204-3205 vs front L3251-3252

[MEDIUM|conf=medium] ASM-3: _analytical_loss returns fractional-loss quantities but the report labels them mW/cm2  >>VERIFY[{'finding_id': 'ASM-3', 'verdict': 'confirmed', 'reasoning': "Quoted code exists verbatim. L6295-6299: `def _analytical_loss(...)` with `Jm = DP.Jph_top*1000*0.93` (=18.3 mA/cm2, since DP.Jph_top=19.65e-3 A/cm2 at L1017), `Vm = Vs*0.88` (V, Vs from expected_voc), `df = GEO.H/(GEO.n_f+1)`, `dBB = GEO.W/(GEO.n_b+1)`. GEO.W/H are in cm (W*10->mm elsewhere, e.g. L6678), so df/dBB are in cm. Return at L6299: `((1/12)*Rs*(Jm/Vm)*df**2, (1/12)*rf*(Jm/Vm)*dBB**2*df, 0.5*rc*(Jm/Vm)*dBB*df/wf)`.\n\nUnit analysis of emitter term (1/12)*Rs*(Jm/Vm)*df**2: [Ohm/sq]*[mA/cm2 / V]*[cm2] = Ohm*mA/V = (V/A)*mA/V = mA/A = 1e-3 (DIMENSIONLESS). The classic Mette/Green emitter fractional loss is (1/12)*Rs*(Jmp_A/Vmp)*S^2 which is dimensionless with Jmp in A/cm2; here Jm is in mA/cm2, so the code returns 1000 x the true fraction. It is NOT a power density: a true mW/cm2 loss would be p_frac * Pmpp = p_frac*(Jm*Vm). Numeric check (Rs=100 Ohm/sq, Jm=18.3 mA/cm2, Vm=1.50 V, S=0.30 cm): code returns 9.16; true fraction=0.0092 (0.92%); true mW/cm2=0.25; so code value is ~37x the true mW/cm2 and ~1000x the true fraction — neither a clean fraction nor a power density.\n\nThe FEM column IS genuine mW/cm2: losses() docstring L6000 says '[mW/cm2]' and returns Pe/Pf/Pc as `/A_*1000` (L6206-6209). Both columns sit under header 'FEM vs Analytical [mW/cm2]' at L6630, with values formatted :.4f side-by-side (L6631-6634), and again at L6735 in _rpt_p7. No compensating conversion exists at the call sites (L6626-6627, L6735-6736). The finger term (rf in Ohm/cm) and contact term (rc in Ohm*cm2) likewise do not reduce to mW/cm2.\n\nIMPACT confirmed: feeds only the report verification table, not efficiency (the efficiency path uses the losses() FEM values), so it does not corrupt results — but it makes the FEM-vs-analytical cross-check apples-to-oranges and can mask/falsely-flag a real FEM discrepancy.", 'corrected_severity': 'medium', 'corrected_location': "_analytical_loss L6295-6299; consumed/labeled at L6629-6635 (header 'FEM vs Analytical [mW/cm2]' L6630), and again at L6735-6736 in _rpt_p7"}] sev->None
   LOC: _analytical_loss L6295-6299; consumed/labeled at L6630 '_txt_tbl(ax1,'FEM vs Analytical [mW/cm2]'...)'

[MINOR|conf=medium] ASM-4: Finger/busbar loss split misattributes 1D edges at finger-busbar intersections
   LOC: losses() metal-loss classification L6059-6063

[MEDIUM|conf=medium] ASM-5: Busbar gets no shape-correction factor cf while fingers do — unverified modeling asymmetry  >>VERIFY[{'finding_id': 'ASM-5', 'verdict': 'confirmed', 'reasoning': 'All three cited code locations exist verbatim. (1) assemble_K_met L1912-1913: `Rsf = rm / (cf * hf) ...  # Finger sheet R` then `Rsb = rm / hf ...  # Busbar sheet R (no CF)`. (2) assemble_K_met_1d L2099-2108: `A_f = w_f_eff * hf; ... Rpl_f = rm / (cf * A_f)` (finger, comment L2098 "with cf") vs `A_b = w_b_eff * hf; ... Rpl_b = rm / A_b` (busbar, comment L2104 "no cf"). (3) assemble_K_met_1d_poly L2516-2523: finger `_chain_rect(..., rm / (cf * A))  # finger: cf 적용` vs busbar `_chain_rect(..., rm / A)  # busbar: cf 미적용`. The asymmetry is deliberate and documented (docstring L2014-2015 "Busbars are treated identically along their (vertical) length, with no shape-correction factor"; L2465 "busbar: cf 미적용").\n\nThe factor `cf` defaults to `shape_cf=0.785` (≈π/4) at L492, documented L483 as "cross-section factor (pi/4 for round, 1 for flat)". The reviewer\'s physics is correct: a printed busbar is also domed/non-rectangular, yet cf=1 is assumed for it, so R_busbar = rm/A is not inflated the way R_finger = rm/(cf·A) is. For the SAME paste cross-section assumption this systematically under-estimates busbar series resistance by ~1/0.785 ≈ 27%, a consistent directional bias toward optimistic FF/efficiency. There is no in-code justification beyond the bare "no CF" comment — the physics assumption (busbar truly rectangular while fingers are domed) is asserted, not defended. So the claim is factually and directionally accurate.\n\nI downgrade severity to minor rather than medium for two reasons grounded in the code: (a) the dome-vs-rectangle deviation matters proportionally less for wide busbars (w_b is typically much larger than w_f), and busbar resistance is usually a small share of total Rs, so the absolute FF impact is bounded except in the narrow-busbar/high-current corner the reviewer themselves flags as conditional; (b) the solver entry points (solve_tandem L3489, solve_single L5018, solve L5268, calc_iv L5630) all default `cf=1.0`, so the asymmetry only manifests when a caller (the GUI at L7218/L11186/L11379 passes 0.785) explicitly supplies cf<1 — meaning the bias is opt-in, not always-on. It is a real, unjustified modeling asymmetry worth documenting/validating against measured busbar geometry, but it is a physics-assumption flag, not a code defect, and its worst-case impact is conditional.', 'corrected_severity': 'minor', 'corrected_location': '2L_FEST_v28_18_wf_wired.py: assemble_K_met L1912-1913; assemble_K_met_1d L2101 (finger, cf) vs L2107 (busbar, no cf); assemble_K_met_1d_poly L2519 vs L2523; cf default shape_cf=0.785 defined L492/L539'}] sev->None
   LOC: assemble_K_met L1912-1913 (Rsb=rm/hf no cf); assemble_K_met_1d L2101 vs L2107; assemble_K_met_1d_poly L2519 vs L2523

[MEDIUM|conf=low] ASM-6: Off-busbar pad/terminal nodes can be metal (ism=True) yet excluded from every 1D chain band  >>VERIFY[{'finding_id': 'ASM-6', 'verdict': 'refuted', 'reasoning': 'The quoted code all exists verbatim: L1667 `tol = max(geo.w_b / 2 + 1e-6, 0.025)`; L2108 `_chain_along(1, geo.bb_x, geo.w_b / 2.0, Rpl_b)`; L3951-3952 `for k in pmk: F[oVm + k] = Vml[k] - Vb`. The factual geometry premise is also true: a pad node at distance (w_b/2 .. 250um) from a terminal is marked is_pad (hence ism) but falls outside the busbar chain band |x-bx|<=w_b/2, so its raw Km row is empty.\n\nHowever, the claim\'s load-bearing failure mode — "a pad/metal node NOT in pmk is a floating metal DOF" — cannot occur in this code. The chain of definitions makes EVERY pad node a pinned node:\n- L1678: `is_metal = is_finger | is_busbar | is_pad`, so is_pad is a subset of ism.\n- L6286/L2928: `isp = is_pad`, `self.pidx = np.where(isp)[0]` — pidx is exactly the pad-node set.\n- L2950-2952: `mmap[gi] >= 0` for every metal node, and pad nodes are metal, so mmap is non-negative for all pad nodes.\n- L3871/L3882: `pmk = [self.mmap[gi] for gi in self.pidx if self.mmap[gi] >= 0]` — therefore pmk contains EVERY pad node, including the entire w_b/2..250um ring the reviewer worries about.\n\nFor each k in pmk the DOF is a hard Dirichlet pin, not a floating node: residual L3951-3952 sets `F[oVm+k] = Vml[k] - Vb`, and the Jacobian rows for bc_front (= oVm + pmk, L3882) are zeroed at L4051-4054 then given a unit diagonal at L4055-4056. So the (possibly empty) Km coupling row is completely overwritten; the node\'s potential is set to Vb regardless of any Km/Gc balance. The reviewer even concedes this pinned case is "harmless."\n\nThe reviewer\'s "metal node NOT in pmk, floating, determined solely by Gc" scenario requires a metal node that is neither finger nor busbar nor pad — which does not exist, since ism is defined as exactly that union. is_busbar (L1652, band w_b/2) matches the busbar chain band (L2108) exactly, and is_finger (L1647, w_f/2) matches the finger band (L2102), so the only metal nodes outside a chain band are pad-ring nodes, and all of those are pinned. There is no all-zero-Km floating metal DOF anywhere.\n\nThe remaining residual concern is purely a modeling nicety: a ring up to 250um radius is hard-pinned to the busbar voltage Vb (the physical terminal/contact potential), which is electrically consistent with the terminal being a contact held at Vb. This is an intentional Dirichlet contact region, not a disconnection or a source of nonphysical potentials or contaminated Pc. No correctness bug.', 'corrected_severity': 'not_a_bug', 'corrected_location': 'classify_nodes L1678 (is_metal = is_finger|is_busbar|is_pad), pmk construction L3871/L3882, Dirichlet BC L3951-3952 + Jacobian row zeroing L4051-4056; FESTSolver.__init__ L2928/L2950-2952'}] sev->None
   LOC: classify_nodes pad tol L1667 (max(w_b/2,0.025)); assemble_K_met_1d band half-width geo.w_b/2 L2108; BC at L3951-3952 / L3882

[MINOR|conf=low] ASM-7: SHORT equipotential bonds are included as edges in the metal-loss quadratic form
   LOC: losses() L6047-6063 (iterates all _Km off-diagonals) vs bond creation in _chain_along L2072-2075

[TRUSTWORTHY|conf=high] ASM-8: Diagonal-absorption in assemble_K_met correctly preserves a symmetric, current-conserving metal Laplacian
   LOC: assemble_K_met L1953-1991

[TRUSTWORTHY|conf=high] ASM-9: assemble_K Galerkin sheet-conductance formula and units are correct
   LOC: assemble_K L1855-1890

[TRUSTWORTHY|conf=high] ASM-10: assemble_K_met_1d R-per-length units and node-band vs cross-section split are self-consistent
   LOC: assemble_K_met_1d L2089-2110

####################################################################################################
### DIM: units | findings:6 verified:2
SUMMARY: I traced every physical input from GUI parsing (_get_params L9722-9738, _apply_grid_design L7831-7848) through the cache/build (_build L3128-3286) into the FEM assemblers (assemble_K, assemble_K_met, assemble_K_met_1d) and the current/loss normalizers (cell_current L5337-5384, losses L5998-6217). The core CGS-cm unit system is internally consistent: resistivity uOhm.cm->Ohm.cm (1e-6), lengths um->cm (1e-4) and mm->cm (0.1), contact resistivity mOhm.cm2->Ohm.cm2 (1e-3), TCO sheet R kept as Ohm/sq. The sheet-resistance formulas (rm/(cf*hf), rm/hf), the 1D line resistance rm/(cf*wf*hf), the Galerkin coefficients, the contact conductance na/rc, and the mA/cm2 current normalization all check out dimensionally and pass magnitude sanity (e.g. Ag 1.6 uOhm.cm at 10 um -> 1.6e-3 Ohm/sq; finger R/L 0.32 Ohm/cm). The unit conversions themselves are applied exactly once with no double-conversion. The one real defect class is area normalization for non-rectangular wafers: current density and efficiency use the bounding-rectangle area while shading uses the true wafer_area(), which is self-inconsistent for pseudo_square/circular cells. A couple of declared Ohm.cm2 parameters (Rs_internal_top/bot) are never wired in.

[MEDIUM|conf=medium] UNITS-1: Efficiency/Jsc normalized by bounding-rectangle area, but shading uses true wafer area -> inconsistent for pseudo-square/circular wafers  >>VERIFY[{'finding_id': 'UNITS-1', 'verdict': 'confirmed', 'reasoning': 'All quoted code exists verbatim and the physics reasoning holds.\n\n(a) Quoted lines verified exactly:\n- L5370: `return np.sum(Jt * self._na) / self.geo.area * 1000` (tandem) and L5384 identical for single (`Jd`).\n- L744: `self.area = cell_w * cell_h  # bounding rectangle area (legacy)`.\n- L5539: `A = float(np.sum(self._na))           # cm² (== self.geo.area, but use _na sum)` — the comment itself asserts the premise.\n- L849: `return (A_f + A_b + A_p - A_ov) / self.wafer_area()`.\n- L5953: `Eff = Pmpp / 100 * 100  # Pmpp in mW/cm2, irradiance = 100 mW/cm2`.\n\n(b) The premise that sum(self._na) == geo.area (= W*H rectangle) is correct. generate_mesh (L1456) seeds the background grid over the FULL rectangle: add() admits any point with -1e-12<=x<=W and -1e-12<=y<=H (L1480), and the base grid is _nested_linspace(0,W)×_nested_linspace(0,H) (L1483-1485). Delaunay (L1574) triangulates the convex hull of a full rectangular point cloud = the entire W×H rectangle. assemble_K (L1855) computes na by summing element area A/3 over all valid elements (L1886-1888), so sum(na) = total triangulated area = W*H = geo.area. There is NO clipping of the mesh to the wafer outline anywhere.\n\nThe two area conventions genuinely disagree for non-square wafers:\n- wafer_area() (L791-802) returns pi*r^2 for circular and W*H - 2*c^2 for pseudo_square, both strictly < W*H.\n- shading_fraction divides metal area by wafer_area() (L849); the DXF _shading even multiplies by (W*H)/wafer_area() (L2359). So shading is scaled to the true wafer.\n- cell_current divides total current by geo.area = W*H rectangle (L5370/5384). calc_iv builds the J-V curve via _eval_internal -> self.cell_current (L5661), Pmpp = Vmpp*Jmpp, Eff = Pmpp/100*100 (L5953). So Jsc and Eff are normalized over the rectangle.\n\nThe chamfer/circle corners are meshed, are NOT under metal (so illum_frac = 1 - metal_frac ~ 1 there, since compute_metal_frac at L1767 only intersects with metal_rects_front and never consults the wafer outline), generate photocurrent, and that current is divided by the rectangle area. wafer_outline_xy_mm (L804) is used only for GUI drawing (L7279+, L10732), a coverage metric (L2359), and an area readout (L8252) — never to clip the mesh or zero generation. So generation is counted over corners that physically do not exist, exactly as claimed.\n\nFor square wafers wafer_area() returns W*H == geo.area, so the conventions coincide and there is no error — also as the reviewer states.\n\n(c) Severity: medium is appropriate. It is a real normalization-convention inconsistency (no crash). Impact is zero for the default square case; for pseudo_square the bias is ~2*c^2/(W*H), typically well under 1% for realistic chamfers; for circular lab wafers it is ~1 - pi/4 ~ 21% and the reported Jsc/Eff are then biased high (current integrated over the rectangle but divided by the rectangle while the meshed corners contribute non-physical generation). Because the worst case requires a non-default wafer_shape setting, medium (not critical) is defensible.', 'corrected_severity': 'medium', 'corrected_location': 'cell_current L5370 (tandem) / L5384 (single) divide by self.geo.area (== W*H rectangle, L744); sum(self._na)==geo.area confirmed at L5539 and by mesh spanning full rectangle (generate_mesh L1480-1485, Delaunay L1574, assemble_K na L1886-1888); shading_fraction L849 and DXF _shading L2359 divide by wafer_area() (L791-802); illum_frac never clipped to wafer outline (compute_metal_frac L1767, illum_frac L2972); Eff via calc_iv L5661/L5953. wafer_outline_xy_mm used only at L2359/L7279+/L8252/L10732.'}] sev->None
   LOC: cell_current L5370/L5384; current_matching_diagnostics L5539; CellGeometry.area L744; shading_fraction L828-849; calc_iv Eff L5953

[MEDIUM|conf=high] UNITS-2: Rs_internal_top/bot (Ohm.cm2 per-pixel vertical Rs) declared but never read; terminal IR-drop uses zero aliases instead  >>VERIFY[{'finding_id': 'UNITS-2', 'verdict': 'partially_confirmed', 'reasoning': 'Verified all quoted lines exist exactly. L1035-1036 define Rs_internal_top/bot (0.0, Ohm*cm2, "Jeon Rs_top: 2.0"/"Rs_bot: 0.2"); L1038-1039 define Rs_lumped_top/bot=0.0; L5643/5646 sum dp.Rs_lumped_top(+bot) into Rs_lumped_total; L5651 applies V_internal - (J_mA*1e-3)*Rs_lumped_total. Grep over the whole file confirms Rs_internal_top/bot appear ONLY at L1035-1036 and are never read — so the dead-parameter claim is TRUE. The unit math at L5651 (mA/cm2 *1e-3 -> A/cm2, *Ohm.cm2 -> V) is correct, as the reviewer concedes, and the same reconstruction is applied consistently at L5774-5777, L5872, L5910.\n\nHOWEVER the reviewer\'s framing of impact/severity is REFUTED. The claim labels Rs_lumped_top/bot as "deprecated backward-compat aliases hard-set to 0.0" and says the IR drop is "fed the wrong (always-zero) variable." That is inaccurate: Rs_lumped_top/bot are the LIVE, user-facing series-Rs inputs. The GUI exposes fields literally labeled "Rs lumped Top"/"Rs lumped Bot" (L7353, L7367) and writes user values into DP.Rs_lumped_top (L9116, tandem), DP.Rs_lumped_bot (L9197), and DP.Rs_lumped_top (L9208, single). So when a user wants to model series resistance, they set "Rs lumped" and the terminal IR drop correctly applies it — FF/Vmpp are NOT over-predicted in the actual workflow. The L1037 comment calling Rs_lumped_* "deprecated" is itself misleading, but functionally they are the active parameter. The variable the IR drop uses is exactly the one the GUI populates, not a zeroed orphan.\n\nSo the genuine defect is narrower than claimed: Rs_internal_top/bot is dead code whose docstring (L1026-1034) advertises a per-pixel internal-Rs feature ("Applied as terminal IR drop") that nothing implements. A user setting dp.Rs_internal_top=2.0 programmatically would get zero effect. That is a real dead-parameter / misleading-doc issue, but it does NOT cause silent FF over-prediction for normal use (the user has a working "Rs lumped" knob). With all defaults 0 there is zero numerical effect. This is a minor cleanliness/documentation defect, not a medium correctness bug.', 'corrected_severity': 'minor', 'corrected_location': 'DiodeParams L1026-1039 (Rs_internal_top/bot dead + misleading docstring/"deprecated" comment); IR-drop path calc_iv L5643-5651 uses the live GUI-wired Rs_lumped_top/bot (GUI fields L7353/7367, setters L9116/9197/9208)'}] sev->None
   LOC: DiodeParams L1035-1039; calc_iv L5643-5651

[MINOR|conf=low] UNITS-3: Rear contact conductance omits metal_frac weighting that the front path applies (na/rc vs na*mf/rc)
   LOC: _build rear L3204-3205 vs front L3251-3252

[TRUSTWORTHY|conf=high] UNITS-4: Contact resistivity uOhm/mOhm and uOhm.cm vs Ohm.cm conversions are correct and applied exactly once
   LOC: _get_params L9722-9738; GUI param_defs L7213-7221; report L6645

[TRUSTWORTHY|conf=high] UNITS-5: FEM sheet-resistance / 1D-line resistance formulas and Galerkin stiffness units are dimensionally correct
   LOC: assemble_K L1866-1879; assemble_K_met L1912-1913; assemble_K_met_1d L2099-2108; b/c/areas L2958-2968

[TRUSTWORTHY|conf=high] UNITS-6: Two independent *1e-4 conversions of the same finger/busbar width entries (mesh geometry vs R-calc) are consistent, not a double conversion
   LOC: _apply_grid_design L7847-7848 (mesh path) vs _get_params L9724-9725 (R path)

####################################################################################################
### DIM: solver | findings:10 verified:5
SUMMARY: The FESTSolver Newton core is, on the whole, carefully constructed: the global system K = K_sheet(Ke) + Gc-coupling + K_metal(Km) + K_rear is assembled with correct signs, the contact conductance Gc = na*mf/rc is dimensionally a conductance (cm^2 / (Ohm*cm^2) = S) and is correctly placed on the TCO<->metal node pair, Dirichlet pad/ground BCs are imposed by zeroing rows and setting a unit diagonal, and the diode Jacobian (dJ/dV exp terms) is added to the right diagonals with mostly correct chain-rule off-diagonals. The build guards against rc=0 (clamp) and missing pads (explicit ValueError), which prevents the most obvious singularities. However there are real defects: a hardcoded VT that disagrees with the module VT in the v29 variant, a uniform bifacial gain in the v29 variant that ignores rear_illum_frac, a convergence test that checks the pre-step residual (lagged by one iterate) uniformly across solvers, and a SHORT/Gc conditioning coupling that becomes ill-conditioned as rc->0 even after the 1e-12 clamp. These distort FF/efficiency mainly in edge regimes (very small rc, Phase-B bifacial via the v29 path, ramped high Rs_junction).

[MEDIUM|conf=high] SOLV-1: Hardcoded VT=0.02568 in v29 Schur solver disagrees with module VT (0.025693)  >>VERIFY[{'finding_id': 'SOLV-1', 'verdict': 'partially_confirmed', 'reasoning': 'The quoted code exists verbatim. L4571: `VT = 0.02568  # kT/q at 25C, module-level (kB·T/q_e)`. The shadowed local VT is used in every diode exponential in this method, e.g. L4583 `e1t = np.exp(np.minimum(Vtop / (dp.n1_top * VT), 80))` and L4598 `e1b = np.exp(np.minimum(Vbot_k / (dp.n1_bot * VT), 80))`, plus L4587, L4599, L4601, L4604-4607.\n\nThe numeric claim is correct. Module VT is defined twice: L108 `q_e = 1.602e-19; kB = 1.381e-23; T = 298.15; VT = kB * T / q_e` and identically L448. That evaluates to (1.381e-23*298.15)/1.602e-19 = 0.0256926 V. The hardcoded 0.02568 is lower by 0.0000126 V = ~0.049%, matching the reviewer\'s "~0.05%." The inline comment "(kB·T/q_e)" is indeed misleading since 0.02568 != kB*T/q_e for T=298.15.\n\nThe dead-code claim is CONFIRMED and stronger than stated. Grep for `_use_v29_constraint_solver` returns exactly one hit, the docstring at L4510 ("Activated via flag `dp._use_v29_constraint_solver = True`"). It is never assigned and never read. Grep for `_solve_tandem_junction_bf_v29` returns only the method definition at L4490 — there is NO call site anywhere. So this method is fully dead code; the flag the reviewer asked to verify is genuinely never set (and the method would not run even if it were, since nothing dispatches to it). The live solvers `_solve_tandem_junction` (L3819) and `_solve_tandem_junction_bf` (L4167) correctly use the module-level VT (L3625-3629, L4281-4291).\n\nMinor citation errors by the reviewer: they cited "0.02585 at L4243" but L4243 is actually `Km_mm = self._Km[...]`, not a VT constant; the 0.02585 constants are at L9367 (`kT = 0.02585`) and L10243 (`VT_ = 0.02585`), both in the GUI/reporting region (>7039) explicitly outside engine scope.\n\nNet: the factual core (hardcoded value, ~0.05% discrepancy, misleading comment, dead code with flag never set) is accurate. But the IMPACT is essentially nil: the code never executes, and even if it did a 0.05% VT shift yields a sub-0.05% Voc/FF change. This is a real but trivial code-quality nit, not a medium-severity correctness bug.', 'corrected_severity': 'minor', 'corrected_location': '_solve_tandem_junction_bf_v29 L4571 (hardcoded VT=0.02568) vs module VT at L108/L448 (=0.0256926). Method is dead code: defined at L4490, never called; flag `_use_v29_constraint_solver` appears only in docstring L4510, never set or read.'}] sev->None
   LOC: _solve_tandem_junction_bf_v29 L4571 (vs module VT at L108)

[MEDIUM|conf=high] SOLV-2: v29 solver applies full bifacial gain uniformly, ignoring rear metal shading (rear_illum_frac)  >>VERIFY[{'finding_id': 'SOLV-2', 'verdict': 'confirmed', 'reasoning': "All claims verified against the source.\n\n(a) Quoted code exists exactly: L4572 `Jph_b_eff = dp.Jph_bot * (1 + dp.bifacial_gain)`.\n\n(b) Comparison paths confirmed verbatim: L4280 (dispatched _solve_tandem_junction_bf) `Jph_b_eff = (ilf + dp.bifacial_gain * self.rear_illum_frac) * dp.Jph_bot`; L4853 (_solve_tandem_bifacial) identical form; L5191 (_solve_single_bifacial) `Jph = (ilf + dp.bifacial_gain * self.rear_illum_frac) * dp.Jph_single`. Diagnostics at L5410 also use `(ilf + bifacial_gain * rilf) * Jph_bot`.\n\n(c) Physics is genuinely wrong, with NO compensation elsewhere:\n - illum_frac (ilf) = 1.0 - metal_frac (L2972) = front unshaded fraction (front metal shading). rear_illum_frac = 1.0 - rear_metal_frac (L2987) = rear unshaded fraction (rear metal shading). bifacial_gain is the rear-illumination multiplier.\n - v29 drops BOTH: it does not multiply the front-illuminated term by ilf, and it applies full bifacial_gain without the rear_illum_frac weight.\n - Internal inconsistency proven within the SAME v29 method: its top cell at L4585 DOES apply ilf (`dp.Jph_top * ilf`), so omitting ilf on the bottom cell is not a deliberate convention.\n - No hidden compensation: Jph_bot is the raw nominal constant (L1046, 19.65e-3 A/cm2). The case-dependent generation scale `_gen_s` is folded into self.illum_frac only (L3286), so it reaches generation ONLY via ilf — which v29's bottom cell bypasses. No other multiplier touches Jph_bot in the v29 path.\n Net effect: over-counts bottom-cell photocurrent under both front and rear metal, inflating Jsc/efficiency wherever metal coverage is non-trivial. Reviewer's physics reasoning is fully correct.\n\nSeverity = medium is appropriate (not critical): the method is dead code. `_solve_tandem_junction_bf_v29` appears only at its definition L4490 and is never called; the activation flag `_use_v29_constraint_solver` appears only in a docstring (L4510) and is never read. So the bug is real but currently unreachable/latent.", 'corrected_severity': 'medium', 'corrected_location': '_solve_tandem_junction_bf_v29 L4572 (Jph_b_eff). Inconsistency visible vs top cell at L4585 in same method; correct reference form at L4280, L4853, L5191.'}] sev->None
   LOC: _solve_tandem_junction_bf_v29 L4572

[MINOR|conf=high] SOLV-3: Convergence test uses the pre-step residual; res_list lags the actual iterate by one
   LOC: solve_tandem L3784-3801; _solve_tandem_junction_bf L4417-4465; _solve_tandem_bifacial L4974-4989; solve_single L5124-5133

[MEDIUM|conf=medium] SOLV-4: SHORT bond scaled by gc_max keeps matrix conditioning tied to rc; rc->1e-12 clamp gives huge conductance ratio  >>VERIFY[{'finding_id': 'SOLV-4', 'verdict': 'partially_confirmed', 'reasoning': 'Code existence: all quoted lines are verbatim. L3142 `rc = max(float(rc), 1e-12)`; L3234 `_gc_peak = float(np.max(self._na[self.ism] * self.metal_frac[self.ism])) / rc`; L2036 `SHORT = max(1.0e4, 1.0e3 * float(gc_max))`. The admitted-stiffness comment is real (L2030-2035): "단 rc→0 에서는 행렬이 stiff 해지므로 일상 실행은 rc>=1e-4 권장." Solver uses bare SuperLU `spsolve` (L3784, 4078, 5124, etc.); grep for rcond/equilibrat/scaling/iterative-refine/cond/warn found NO conditioning guard or user-facing warning tied to small rc (only DXF-import and FF-range warnings exist).\n\nMechanism is genuinely correct. `_na` (assemble_K L1885-1888) is nodal area in cm^2 = sum(elem_area/3). `_gc_peak = max(na*mf)/rc`, so SHORT = 1e3*max(na*mf)/rc. The sheet stiffness coeff is `1/(4*A*Rs)` (L1867) and the 1D finger conductance is `1/(R_per_len*dl)` (L2083) — both O(1e-2..1e2). At the rc=1e-12 clamp the Gc/SHORT entries dwarf these by ~10-13 orders of magnitude, so the K dynamic range is exactly what the reviewer describes. The reviewer\'s na~1e-4 is plausibly even conservative on fine meshes (na can be 1e-2..1e-1, making the ratio worse). So the conditioning concern is real, the code\'s own comment concedes it, and nothing surfaces a warning when rc is driven to 1e-12.\n\nWhere I DOWNGRADE from the reviewer\'s framing (why "partially"): the claimed IMPACT — polluted Rs/FF/efficiency at the *legitimate* GUI input rc=0 — is not actually demonstrated and is materially overstated for the realistic operating point. (1) The convergence test is on the RESIDUAL F (e.g. L5130 `r=np.max(np.abs(F))`, break at <1e-10) and the Newton damping caps dV to 0.3 (L5125-5128). A stiff K does NOT bias the converged V field at a *true* solution: at convergence F≈0 regardless of K\'s condition number; ill-conditioning degrades the accuracy of each dV step (and can stall convergence / inflate iteration count), not the fixed point. Round-off "converging to a slightly wrong V field" is only a real risk if the solve fails the residual tolerance, which the loop checks; there is no evidence of an accepted-but-wrong solution. (2) The truly extreme regime (rc≈1e-12) only occurs if the user literally enters rc=0 ("ideal contact"); for any normal rc (default ~1e-3..1e-1 Ω·cm²), gc_max~1e-3 and SHORT pins to its floor 1e4 (the comment notes "기본 rc에선 ... SHORT=1e4 그대로"), so the matrix is well-scaled and the bit-identical path holds. The harmful case is a corner input, not the default. (3) The 1e-12 clamp is a deliberate, documented design choice that prevents a hard inf/NaN (Gc=na/0); it trades a crash for a stiff-but-bounded matrix and even pulls SHORT up proportionally so the equipotential bond still dominates Gc (the mesh-independence property the bond exists to protect is preserved). So the most-feared downstream symptom (silent wrong efficiency) is mitigated by the residual-based convergence check and is confined to a degenerate user input. The real, defensible defect is the absence of any warning/guard steering the user off rc below ~1e-4 (the code only buries the recommendation in a comment) — a robustness/UX gap, not a silent quantitative-correctness bug in normal operation. Hence medium is too high; minor is appropriate.', 'corrected_severity': 'minor', 'corrected_location': '_build L3142 (rc clamp) and L3234 (_gc_peak); assemble_K_met_1d L2036 (SHORT) with admitted-stiffness comment L2030-2035; bare spsolve at L5124/3784/4078 with no conditioning guard or rc-too-small warning anywhere'}] sev->None
   LOC: _build L3142 (rc clamp), L3234 (_gc_peak), assemble_K_met_1d L2036 (SHORT)

[MEDIUM|conf=medium] SOLV-5: Patterned-rear fallback grounds the rear plane at FRONT pad node indices  >>VERIFY[{'finding_id': 'SOLV-5', 'verdict': 'refuted', 'reasoning': 'The quoted code exists verbatim at solve_tandem L3571-3577 and solve_single L5047-5053. The geometric observation is correct in isolation: self.pidx (L2928 = np.where(isp)[0]) is the FRONT pad/probe node-index set, and the rear plane Vr is defined over the same N mesh nodes, so grounding Vr at self.pidx would misplace the rear ground onto the front-pad (x,y) footprint. HOWEVER the claimed trigger ("patterned rear with missing rear probe") is effectively unreachable, so no FF distortion occurs in practice. Evidence: (1) rear_mode is only ever assigned \'full_area\' or \'bifacial\' (L756: rear_mode=\'full_area\' if rear is None else \'bifacial\'; forced \'full_area\' at L10618). The value \'patterned\' is NEVER assigned anywhere; it only appears inside defensive \'in (\\"bifacial\\",\\"patterned\\")\' membership tests. (2) To hit the fallback, rear_mode must be non-full_area (i.e. \'bifacial\') AND rear_pidx must be None/empty. But rear_mode=\'bifacial\' requires geo.rear to be a real GridDesign (L756; GUI L8046 passes rear=rear only when rear_mode==\'bifacial\', with n_fingers>=1, n_busbars>=1). A valid geo.rear makes classify_nodes (L1681-1716) build is_rear_metal/is_rear_pad with explicit safety fallbacks (L1713-1716 guarantee >=1 rear pad node). Non-empty isrm makes _Krm (L3193) and _J_static_tandem_bf/_J_static_single_bf (L3395/L3480) non-None, so the bifacial dispatch fires FIRST (solve_tandem L3537-3548 -> _solve_tandem_junction_bf/_solve_tandem_bifacial; solve_single L5027-5029 -> _solve_single_bifacial) and the single-layer fallback at L3577/L5053 is never reached. (3) The DXF import path the reviewer explicitly flagged uses classify_nodes_poly, which returns isrm=None,isrp=None (L2399, docstring: rear treated as full_area) AND builds GEO with no rear arg (L8877 CellGeometry(cell_w,cell_h,front=front)) => rear_mode=\'full_area\' => takes the FIRST branch (np.arange(N), the correct ideal full back contact), NOT the fallback. Therefore the else branch (rear_probe_idx = self.pidx) is dead code under all reachable configurations; the construction logic and classify_nodes safety nets prevent the only state that would reach it. It is a defensive singularity-avoidance crutch as the comment says, with no observable effect on rear Rs/FF in any real run.', 'corrected_severity': 'not_a_bug', 'corrected_location': 'solve_tandem L3575-3577 and solve_single L5051-5053 (else-branch fallback `rear_probe_idx = self.pidx`); reachability gated by L756 rear_mode assignment, L3181/L3343/L3429 _Krm/_J_static_*_bf construction, dispatch guards at L3537-3548 and L5027-5029, and classify_nodes_poly L2399 / DXF GEO build L8877'}] sev->None
   LOC: solve_tandem L3571-3577; solve_single L5047-5053

[MEDIUM|conf=high] SOLV-6: Stagnation 'converged' escapes accept residuals far above the nominal tolerance (one with NO ceiling)  >>VERIFY[{'finding_id': 'SOLV-6', 'verdict': 'partially_confirmed', 'reasoning': "All four quoted code blocks exist verbatim and the reviewer's function-name mapping is correct. solve_tandem L3804-3807: `if max(recent)/max(min(recent),1e-20) < 1.05 and r < 1e-6: break` (primary tol r<1e-10 at L3800). _solve_tandem_junction L4146-4149: `... < 1.05 and r < 5e-6: break` (primary r<1e-7 at L4143). _solve_tandem_junction_bf L4470-4473: `if len(res_list)>=12: recent=res_list[-8:]; if max(recent)/max(min(recent),1e-20) < 1.10: break` — confirmed there is NO `and r < ...` residual ceiling (primary tol r<1e-6 at L4464). _solve_tandem_bifacial L4994-4997: `... < 1.05 and r < 5e-6: break` (primary r<1e-7 at L4988). So the central factual claim — that _solve_tandem_junction_bf's stagnation escape accepts a flat residual of ANY magnitude while every other tandem solver (including the _v29 variant at L4740-4742, r<5e-6) caps it — is TRUE. This is not dead code: solve_tandem dispatches to _solve_tandem_junction_bf at L3522/L3529/L3540 for the bifacial/patterned + Phase B path, so it is an active primary solver.\\n\\nHowever, the reviewer overstates 'reported as converged' / 'silently' and missed compensating gates. (1) The caller wrapper at L5310-5312 reads `residuals[-1]` and sets `failed = (not finite) or final_residual > 1e-5`; if prolongation was used and it failed, it re-solves without the seed (L5313-5321), and records `final_newton_residual` (L5327). (2) _iv_health L252-253 flags any `final_newton_residual > 1e-5` as status CHECK, and L261-266 flags `phase_b_native_kcl_max_A > 1e-5`. So an escape that returns r~1e-3 would be flagged (and conditionally retried), not silently passed off as solved. The gaps the reviewer is right about: these are reporting/single-retry gates, not hard rejection — the biased I-V point still enters the curve and the fit; and `final_newton_residual` plus the phase_b KCL diagnostic (computed via current_matching_diagnostics on self._last_solve_result, L5966-5978) only cover the LAST solved point, so a stalled INTERIOR I-V point with no prolongation seed can pass into the Vmpp/FF fit without an individual flag. The step cap (cap=0.10, L4429), Vtop clip (L4457-4459) and 8-iteration flatness requirement make a large-magnitude flat stall less likely than the near-1e-6-floor case the code comment targets, but nothing structurally prevents accepting r above 1e-6.\\n\\nNet: the defect (uniquely missing ceiling in an active solver) is real; the worst-case impact is real but partially mitigated/observable, so 'partially_confirmed' rather than fully confirmed.", 'corrected_severity': 'medium', 'corrected_location': '_solve_tandem_junction_bf L4470-4473 (no residual ceiling; function defined at L4167, dispatched from solve_tandem at L3522/L3529/L3540); compare ceilinged escapes at solve_tandem L3804-3807 (r<1e-6), _solve_tandem_junction L4146-4149 (r<5e-6), _solve_tandem_junction_bf_v29 L4740-4742 (r<5e-6), _solve_tandem_bifacial L4994-4997 (r<5e-6); downstream gates at L5310-5312 and _iv_health L252-266'}] sev->None
   LOC: solve_tandem L3804-3807; _solve_tandem_junction L4146-4149; _solve_tandem_bifacial L4994-4997; _solve_tandem_junction_bf L4470-4473

[MINOR|conf=low] SOLV-7: Generation rescale by global shading ratio decouples optical shade area from J0-under-metal area at off-design widths
   LOC: _build L3277-3286; solve_tandem L3579-3600; cell_current L5343-5384

[TRUSTWORTHY|conf=high] SOLV-8: Contact conductance Gc = na*mf/rc is dimensionally correct and placed on the right TCO<->metal node pair with correct 2x2 stencil signs
   LOC: _build L3251-3252, L3316-3324; solve_tandem F-coupling L3715-3721

[TRUSTWORTHY|conf=high] SOLV-9: Local-to-global DOF index mapping (mmap/rear_mmap) and block offsets are consistent across all solver variants
   LOC: __init__ mmap L2949-2952, rear_mmap L2941-2943; offsets in solve_tandem L3551, _solve_tandem_junction_bf L4185-4190; static Jacobian L3309-3347

[TRUSTWORTHY|conf=high] SOLV-10: Dirichlet BC imposition (zero row + unit diagonal) is correct and pad/probe existence is guarded against singularity
   LOC: solve() guard L5277-5290; BC application solve_tandem L3774-3781; pad RHS via F[oVm+k]=Vml[k]-Vb L3722-3723

####################################################################################################
### DIM: iv_losses | findings:9 verified:5
SUMMARY: The IV extraction (Pmax search, Vmpp/Jmpp, FF, Eff) is mostly sound and self-consistent in the terminal-voltage frame, and the resistive-loss buckets are now evaluated as model-exact quadratic forms (V^T K V) on the same operators the solver uses, which is genuinely correct. However the headline "Tier 1 energy-balance closure" is physically wrong: it equates electrical Pmpp + a handful of electrical loss buckets to the 100 mW/cm2 OPTICAL input, ignoring the dominant ~60 mW/cm2 of thermalization/below-bandgap optical loss, so the residual can never be <5% for a real tandem and the reported "PASS" is meaningless. Shading is applied to current exactly once (via illum_frac=1-metal_frac in the solver) and then re-added once as a P_shade bucket, which is correct for a budget but is mis-normalized and slightly under-counted; the non-MPP fallback for P_shade also has a 1000x unit error. Several loss-attribution details (recomb/shunt sign of "loss", FF unaffected by lumped Rs in some branches) need verification.

[CRITICAL|conf=high] IVL-1: Energy-balance closure equates electrical output+losses to OPTICAL 100 mW/cm2 (thermalization gap missing) — 'Tier 1 PASS' is unachievable/meaningless  >>VERIFY[{'finding_id': 'IVL-1', 'verdict': 'confirmed', 'reasoning': "The quoted code exists verbatim. In _rpt_p7 (L6695-6709): L6699 `P_AM15 = 100.0  # mW/cm²`; L6700 `P_out_b = iv_b['Pmpp']`; L6701 `P_loss_total_b = Pt_b + Ps_b + Psh_b + Prec_b + Pj_b` (with Pt_b = Pe_b+Pf_b+Pc_b from L6693); L6703 `closure_b = P_out_b + P_loss_total_b`; L6704 `residual_b = abs(closure_b - P_AM15)/P_AM15*100`. Status badge L6727 `'PASS (<5%)' if residual_b < 5.0 else 'CHECK (>5%)'`.\n\nEvery term in the closure is an ELECTRICAL power in mW/cm². Confirmed units: Pmpp is mW/cm² (L5953 `Eff = Pmpp/100*100  # Pmpp in mW/cm2, irradiance = 100 mW/cm2`); losses() returns all terms `/A_*1000` → mW/cm² (L6206-6217). The closure is fed from loss_b (L9812-9816): Pe_b=loss_b['Pe'], Ps_b=loss_b['P_shade'], Psh_b=loss_b['P_shunt'], Prec_b=loss_b['P_recomb'], etc. — pure electrical loss buckets. There is NO optical/thermalization/below-bandgap term anywhere in losses() (L5998-6217): the function only computes ohmic emitter/rear (Pe, P_rear, V^T K V), metal grid (Pf), contact (Pc), shading (P_shade), shunt (P_shunt), recomb (P_recomb), and interlayer junction (P_junction). The model starts from a FIXED photocurrent Jph_top=19.65e-3 A/cm² (L1017), not a spectrum, so the dominant ~60 mW/cm² of photon energy above-Eg / sub-bandgap loss is structurally absent.\n\nNumeric check (my own bisection on the model's own diode params): Vt~1.39 V, Vb~0.90 V → tandem Voc~2.29 V; with Jsc~19.65 mA/cm² and FF~0.78-0.82, Pmpp~35-37 mW/cm². The electrical losses (shading ~1-3 mW, resistive/shunt/recomb each a few mW) bring closure to only ~40-42 mW/cm². Compared to P_AM15=100, residual ≈ 58-66% — far above 5%. This matches the reviewer's claimed ~66% (they used a slightly more conservative Voc/Pmpp). So the residual is structurally ~55-66% and the badge will essentially always read 'CHECK (>5%)', never PASS, for any physical cell. The check is a category error (electrical generation budget compared against optical irradiance) and is meaningless as a self-consistency validation. The author's own adjacent comments (L6697-6698: 'crude upper bound' Jph*Voc, then 'Better: P_in = AM1.5G') confirm the conflation.\n\nMitigating nuance (does not refute the finding): this is confined to the reporting/validation page _rpt_p7; it does NOT corrupt the solver's electrical outputs (Pmpp, loss decomposition are computed correctly elsewhere). Also note the badge is not falsely GREEN by default (residual>5% triggers the red 'CHECK' path), so a casual reader sees a failing check rather than a false PASS — but the framing as an 'energy balance' that should close to 100% is wrong, and anyone trusting the Tier 1 'closure/residual' as a validity signal is misled about whether the electrical loss decomposition actually accounts for the budget.", 'corrected_severity': 'critical', 'corrected_location': '_rpt_p7 L6695-6732 (closure L6699-6709, badge L6712-6732); loss buckets from losses() L5998-6217 (no optical/thermalization term); data populated at L9810-9856; Jph_top fixed at L1017; Pmpp units at L5953'}] sev->None
   LOC: _rpt_p7 L6695-6709 (closure), uses losses() L5998-6217 and iv['Pmpp']

[MEDIUM|conf=high] IVL-2: P_shade non-MPP fallback branch has a 1000x unit error (mW vs W/cm2) vs the MPP branch  >>VERIFY[{'finding_id': 'IVL-2', 'verdict': 'confirmed', 'reasoning': 'The quoted code exists verbatim at L6075-6080. The unit reasoning is correct on every point.\n\nUNITS VERIFIED:\n- L1017: `Jph_top = 19.65e-3 # A/cm2` — explicit comment confirms Jph_top is in A/cm2.\n- L5338 docstring "Total cell current [mA/cm2]"; L5370/L5384 `cell_current` returns `... / self.geo.area * 1000` (A/cm2 -> mA/cm2). So `Jmpp = Js[im]` (L5905) and hence `iv[\'Jmpp\']` is in mA/cm2. Corroborated by `_V_terminal` (L5651) multiplying J by `1e-3` to convert mA/cm2 -> A/cm2 for the IR drop, and the comment L5831 "J in mA/cm² → ×1e-3 to A".\n- L6000 docstring: losses() returns the breakdown in [mW/cm2].\n\nMPP branch (L6076): `shade_frac * Jmpp[mA/cm2] * Vmpp[V]` = mW/cm2 — consistent with documented return unit and sibling terms, no /1000. Correct.\n\nFallback branch (L6078-6080): `Jmpp_est = dp.Jph_top * 1000 * 0.95` = (A/cm2 * 1000) = mA/cm2 (~18.67), same units as the MPP-branch Jmpp. `Vmpp_est` in V. So `shade_frac * Jmpp_est * Vmpp_est` is already mW/cm2, and the trailing `/ 1000` makes it W/cm2 — exactly 1000x too small. The two branches are genuinely unit-inconsistent, as claimed.\n\nIMPACT — verified the dormancy claim: grep for `.losses(` returns exactly three callers (L9812, L9823, L10535), and ALL THREE pass `Vmpp=iv[\'Vmpp\'], Jmpp=iv[\'Jmpp\']` (L9812/9823 confirm the GUI path; L10535-10536 is the rho-sweep path and also passes them). The buggy else-branch is therefore never reached by any in-file caller — it is fully latent. The default signature `Vmpp=None, Jmpp=None` (L5999) means a future/external `losses(result, ...)` call omitting them would silently drop shading from the budget by 1000x, so the bug is real but currently dead code.', 'corrected_severity': 'minor', 'corrected_location': 'L6078-6080 (the else/fallback branch of the P_shade computation in losses(), specifically the trailing `/ 1000` on L6080)'}] sev->None
   LOC: losses L6075-6080

[MEDIUM|conf=medium] IVL-3: P_shade uses the already-shaded Jmpp, so the shading bucket under-counts the true optical loss  >>VERIFY[{'finding_id': 'IVL-3', 'verdict': 'confirmed', 'reasoning': 'Quoted code is exact. losses() L6072-6076: `wb_eff = self.geo.w_b if wb is None else float(wb)`; `shade_frac = self.geo.shading_fraction(wf, wb_eff)`; `if Vmpp is not None and Jmpp is not None: P_shade = shade_frac * Jmpp * Vmpp`. Returned as \'P_shade\' at L6210.\n\nGeneration IS already shaded in the solver: `_build` sets `self.illum_frac = 1.0 - self.metal_frac` (L2972) and cell_current uses `_gen = ilf` (L5344, L5351) so the FEM photocurrent is `Jt = _gen*dp.Jph_top - ...` (L5369) / single `Jph_eff = (_gen + ...)*dp.Jph_single` (L5382). calc_iv takes `Jmpp = Js[im]` (L5905) from that solve, so the Jmpp passed into losses() (callers L9812/L9823 pass `Jmpp=iv_b[\'Jmpp\']`) is the POST-shading terminal current. The reviewer\'s premise is correct.\n\nPhysics of the bucket is biased low. The waterfall (L10300) defines `PCE_ref = PCE_act + L_opt + L_rec + L_sh + L_res` with `L_opt = Ps_b` (L10289), i.e. the optical bucket is meant to be the power recoverable by removing shading. Removing shading raises generation from ~(1-shade_frac)·Jph back to ~Jph, so the recoverable power at Vmpp is ~shade_frac·Jph·Vmpp ≈ shade_frac/(1-shade_frac)·Jmpp·Vmpp, not shade_frac·Jmpp·Vmpp. Using the shaded Jmpp under-counts by factor ~(1-shade_frac). Units are fine (mA/cm2·V = mW/cm2 = %abs at 100 mW/cm2). It is counted exactly once (shade only in L_opt; recomb/resistive buckets use the already-shaded node currents), so no double-count — the reviewer\'s "good, not double counted" is right.\n\nSecond sub-claim also holds: shade_frac comes from shading_fraction (L828-849, analytic A_f+A_b+A_p-A_ov over wafer_area) while the generation deficit the solver applied comes from metal_frac via compute_metal_frac (L1767-1794, mesh tri-rect intersection of metal_rects_front). Two different estimators of metal coverage; close but not guaranteed equal, so P_shade need not exactly equal the solver\'s generation deficit.\n\nSeverity: I downgrade to minor. shade_frac is only a few percent, so the bias is ~(1-shade_frac) ≈ a few percent OF a bucket that is itself a few %abs — net error is sub-0.1 %abs. It affects only the optical-vs-electrical split in a GUI reporting waterfall, NOT the primary metrics (Pmpp/FF/Eff computed independently in calc_iv). The code itself documents PCE_ref as a first-order reconstruction (L10298: "1차(first-order) 복원"). Real physics inaccuracy, but small magnitude, secondary reporting only, self-disclaimed as first-order.', 'corrected_severity': 'minor', 'corrected_location': 'losses L6072-6076 (P_shade = shade_frac*Jmpp*Vmpp), with Jmpp originating post-shading from cell_current _gen=illum_frac (L5344/5351/5369/5382) via calc_iv L5905; illum_frac=1-metal_frac at L2972; shade_frac from shading_fraction L828-849 vs solver deficit from compute_metal_frac L1767-1794; consumed in waterfall L10289/L10300'}] sev->None
   LOC: losses L6072-6076; generation in cell_current L5351/L5369 and solver via illum_frac=1-metal_frac (L2972)

[MEDIUM|conf=medium] IVL-4: FF/Voc are computed in the terminal frame for lumped Rs, but the Rs lumped-drop path and the Voc-refinement are mutually exclusive — Voc refinement is SKIPPED whenever lumped Rs>0  >>VERIFY[{'finding_id': 'IVL-4', 'verdict': 'refuted', 'reasoning': 'The quoted code exists verbatim: L5863 guard `if Voc_bracket is not None and Rs_lumped_total <= 1e-6:`, the 8-step terminal-frame bisection at L5864-5892, and L5952 `FF = Pmpp / (Jsc * Voc) * 100 ...`. However, the reviewer\'s premise — that for Rs>0 "Voc is taken only from the coarse linear interpolation" with a 10-20 mV underestimate — is FALSE.\n\nTrace of the Rs>0 path:\n1. `Voc_internal` is computed by ACTUAL-SOLVER BISECTION, not coarse interp. L5740-5747 finds the sign change in the internal-frame current `Js_coarse` (which is Rs-independent) and calls `_bisect_root(lambda v: _eval_internal(v)[0], Vs_coarse[k], Vs_coarse[k+1])`. `_bisect_root` (L5679) uses `xtol=5e-5` = 0.05 mV with real `self.solve` calls. This runs identically for Rs=0 and Rs>0. This is the SAME 0.05 mV accuracy the v28.1 FIX block targets (its loop also stops at `b - a < 5e-5`, L5886).\n2. For Rs>0, an EXACT anchor point is injected into the curve: L5836-5837 append `Vs = float(Voc_internal)` with `Js = 0.0`. Since J=0, the Rs drop `J*1e-3*Rs = 0`, so terminal V == internal V == Voc_internal at this node.\n3. The Voc bracket loop L5853-5857 then finds the sign change where `Js[k+1] <= 0`; that node IS the injected J=0 anchor, so `np.interp(0, [Js[k+1]=0, Js[k]], [Vs[k+1]=Voc_internal, Vs[k]])` returns exactly Voc_internal.\n\nSo in the Rs>0 case Voc is the bisected (0.05 mV) value, NOT a coarse-grid interpolation. The v28.1 terminal-frame refinement is gated off for Rs>0 because it is redundant: the equivalent accuracy is achieved via the pre-injected bisected anchor (L5836). The reviewer even concedes "with lumped Rs the J=0 (Voc) point is Rs-independent, so the refinement SHOULD still be valid" — it IS valid; the code just delivers it through a different (and physically correct) mechanism. The claimed 0.5-1% FF systematic bias does not exist; there is no Voc under-resolution in the Rs>0 path.', 'corrected_severity': 'not_a_bug', 'corrected_location': 'calc_iv: guard L5863; Voc_internal bisection L5740-5747 (via _bisect_root L5679 xtol=5e-5); exact J=0 anchor injection for Rs>0 at L5836-5837; bracket interp L5853-5857; FF L5952'}] sev->None
   LOC: calc_iv L5863 (guard), L5859-5893 (refine), L5950-5952 (FF)

[MINOR|conf=medium] IVL-5: Tier 2 'analytical' validation uses a fixed Jm/Vm heuristic (0.93/0.88) decoupled from the actual MPP, so agreement is partly tautological / can mask real errors
   LOC: _analytical_loss L6295-6299; consumed _rpt_p7 L6735-6742

[MEDIUM|conf=low] IVL-6: P_shunt and P_recomb are summed into the same closure bucket as ohmic losses, but recomb 'power loss' Jr*V double-uses voltage already accounted in Voc deficit — verify no overlap with the (broken) P_in baseline  >>VERIFY[{'finding_id': 'IVL-6', 'verdict': 'partially_confirmed', 'reasoning': 'Quoted code exists verbatim. L6166-6172: P_recomb_top=np.sum(Jr_top*Vtop*self._na); Jr_bot=dp.J01_bot*(e1b-1)+dp.J02_bot*(e2b-1); P_recomb_bot=np.sum(Jr_bot*Vbot*self._na); P_recomb=P_recomb_top+P_recomb_bot. Shunt at L6096-6101. Closure _rpt_p7 L6701: P_loss_total_b=Pt_b+Ps_b+Psh_b+Prec_b+Pj_b; closure_b=P_out_b+P_loss_total_b; residual vs P_AM15=100.0 (L6699-6704).\n\nREFUTED part (the headline): "Jr*V double-uses voltage already accounted in Voc deficit" is overstated and the reviewer self-retracts it in the body ("These are legitimate as the power consumed by dark recombination/shunt at the MPP operating point"). There is no double-count. Voc deficit is the V=Voc,I=0 point; Jr*V at MPP is a distinct, additive LOCAL dissipation that Pmpp does not contain. The recomb formula and shunt formula are physically correct operating-point dissipations.\n\nCONFIRMED part (description of code): There is genuinely no node-wise current-conservation identity (J_gen=J_diode+J_shunt+J_lateral+J_load) asserted or tested anywhere — searched, none exists; recomb_currents (L6239) only reports currents, no balance. The closure targets the constant P_AM15=100.0, not the actual generation budget integral(Jph*V), and the code comment at L6697-6699 even calls the Voc-based bound "crude" before discarding it for 100.\n\nMISDIAGNOSED part (the impact): The reviewer implies the recomb/shunt buckets are the source of the unattributed remainder. They are not. The dominant residual against 100 is structural/unavoidable: AM1.5G 100 mW/cm2 is OPTICAL input, but the cell converts only a fraction to the electrical generation budget; the difference (thermalization, sub-bandgap transmission, quantum defect) is an optical loss the waterfall never models. That residual would be ~66% even with perfect recomb/shunt terms. So the gap is the missing optical bucket + the wrong (100) baseline, i.e. this finding is fundamentally a dependent/duplicate of the already-separately-filed broken-baseline issue (IVL-1), which the reviewer explicitly cross-references. The cited lines compute legitimate quantities correctly; nothing in L6096-6101/L6161-6180/L6701 is a computational bug.', 'corrected_severity': 'minor', 'corrected_location': 'c:\\Users\\0363q\\Desktop\\2L-FEST\\2L_FEST_v28_18_wf_wired.py losses() L6096-6101 (P_shunt), L6161-6172 (P_recomb), closure _rpt_p7 L6699-6709 (P_AM15=100.0 baseline; residual hides unmodeled optical/thermalization losses)'}] sev->None
   LOC: losses L6096-6101 (shunt), L6161-6180 (recomb), closure _rpt_p7 L6701

[MINOR|conf=medium] IVL-7: extract_rs_gsh slope estimates use raw 2-point finite differences at curve ends — fragile and sign-dependent on sweep direction/duplicates
   LOC: extract_rs_gsh L6222-6234

[TRUSTWORTHY|conf=high] IVL-8: Metal grid loss correctly evaluated edge-by-edge on the SAME 1D network as the solver (model-exact)
   LOC: losses L6047-6065

[TRUSTWORTHY|conf=high] IVL-9: Pmax search and FF/Eff are computed self-consistently in the terminal (Rs-corrected) frame
   LOC: calc_iv L5895-5953

####################################################################################################
### DIM: tandem | findings:8 verified:5
SUMMARY: The 2-terminal series constraint is genuinely and correctly enforced in the active full-area and bifacial FEM solvers: at each node the top and bottom subcells share the same current via the residual (Jt - Jb)*na = 0 (L3725, L4922) and their diode voltages add through Vbot = Ve - Vtop - Vr (L3622, L4841), so the FEM solve truly couples the subcells in series rather than treating them independently. Current matching is handled implicitly (it emerges from the node-wise KCL, not assumed), and the optional perovskite/Si tunnel-junction is modeled both as a vertical contact R (Rc_junction, Phase A) and a lateral interlayer sheet-R plane (Rs_junction, Phase B). However, there are real problems: a sign inconsistency in the interlayer KCL source term between the two Phase-B solvers, a bifacial-gain formula divergence (one dead solver applies a uniform 1+gain ignoring shading), the documented per-subcell internal series resistances (Rs_internal_top/bot, Jeon's Rs) are completely orphaned, and the default DiodeParams yield a non-credible ~39.7% / Voc=2.27V tandem. For a credible full-tandem efficiency claim the front perovskite/TCO contact + bulk vertical transport (Rs_internal_top) and a spectrally-split top/bottom photocurrent are the main missing pieces.

[CRITICAL|conf=high] TANDEM-1: Interlayer KCL source term has OPPOSITE sign in the two active Phase-B solvers  >>VERIFY[{'finding_id': 'TANDEM-1', 'verdict': 'refuted', 'reasoning': "The quoted code is accurate. full_area `_solve_tandem_junction` L3960: `F[oVint:oVint+N] = self._K_junc @ V_int - It + Ib`. bifacial `_solve_tandem_junction_bf` L4316: `F[oVint:oVint+N] = self._K_junc @ V_int + It - Ib`. The two source terms are genuinely opposite, both with `+K_junc@V_int`, so they DO converge to different V_int planes for finite Rs_junction. The diagnostic branches on rear_mode (L5447-5452) to match each sign. All of that is verified.\\n\\nWHERE THE CLAIM FAILS: the reviewer asserts full_area is physically correct and the bifacial sign is reversed. That conclusion is backwards (or at minimum unproven) once you check it against the codebase's OWN universal KCL sign convention rather than an abstract argument. The file uses one consistent 2-plane pattern everywhere: for a diode current I flowing from an upper plane to a lower plane, the UPPER plane residual is `K@V - I` and the LOWER plane residual is `K@V + I`. Evidence (all verified): single-cell `solve_single` L5090/L5099 `Ke@Ve - Id` / `Kr@Vr + Id`; `_solve_single_bifacial` L5201/L5213 `Ke@Ve - Id` / `Kr@Vr + Id`; full_area tandem L3944/L3956 `Ke@Ve - It` / `Kr@Vr + Ib`; bifacial tandem L4301/L4318 `Ke@Ve - It` / `Kr@Vr + Ib`. Both tandem solvers AND both single-cell solvers agree on these emitter/rear signs with no exception.\\n\\nThe interlayer plane is simultaneously the LOWER plane for the top diode (top cell sits above it -> contributes +It) and the UPPER plane for the bottom diode (Si sits below it -> contributes -Ib). Applying the file's universal pattern, the consistent interlayer residual is `K_int@V_int + It - Ib`, i.e. the BIFACIAL form at L4316. The full_area form `-It + Ib` at L3960 is the one that DEVIATES from the convention used by the other five KCL residuals in the file. Each solver is also internally self-consistent (its Jacobian matches its own residual sign: full_area dF_Vint/dVint = Kint + dIb at L4028-4033 for the +Ib term; bifacial dF_Vbot/dVbot = Kint - dIb at L4362-4363 for the -Ib term), so neither is failing to converge to the root of its own equation.\\n\\nThe reviewer's 'current must flow OUT so K_int@V_int>0 requires source=It-Ib>0' argument silently assumes K@V means 'net lateral OUT', the OPPOSITE Laplacian-sign convention from the one the code actually realizes in its emitter/rear equations; so it does not establish that full_area is the correct one. Thus the IMPACT as written ('any Phase-B BIFACIAL run solves with the wrong sign; full_area unaffected') is refuted/inverted: the convention-consistent solver is the bifacial one, and if either sign is wrong it is the full_area sign.\\n\\nResidual real concern (not the bug as filed): two active solvers genuinely use opposite interlayer source signs, so full_area vs bifacial Phase-B will predict different local Vbot/recomb/FF at finite interlayer R, and they cannot both be right. That is a legitimate consistency issue worth a maintainer's look, but it is the OPPOSITE direction from the claim and does not rise to a confirmed critical bug in the bifacial path as described.", 'corrected_severity': 'not_a_bug', 'corrected_location': '_solve_tandem_junction L3960 (full_area) vs _solve_tandem_junction_bf L4316 (bifacial); convention reference: solve_single L5090/L5099, _solve_single_bifacial L5201/L5213, shared emitter/rear L3944/L3956 and L4301/L4318'}] sev->None
   LOC: _solve_tandem_junction L3960 (full_area) vs _solve_tandem_junction_bf L4316 (bifacial)

[CRITICAL|conf=high] TANDEM-2: Documented per-subcell internal series resistance (Jeon Rs_top/Rs_bot) is never applied  >>VERIFY[{'finding_id': 'TANDEM-2', 'verdict': 'confirmed', 'corrected_severity': 'medium', 'corrected_location': 'DiodeParams.Rs_internal_top/Rs_internal_bot at L1035-1036 (docstring L1026-1034); referenced nowhere else. calc_iv terminal IR drop uses only Rs_lumped_total at L5643/5646 and L5650-5651.', 'reasoning': 'Quoted code exists verbatim at L1035-1036 with the Jeon Rs_top=2.0/Rs_bot=0.2 comments, and the docstring L1026-1034 promises application as a terminal IR drop V_terminal = V_solver - J x Rs_internal. Full-file grep for Rs_internal_top/Rs_internal_bot returns only 3 hits: docstring L1033 plus the two definitions L1035-1036. They are never read by any diode equation (subcell_iv L1188-1199 uses only Rsh), assembly, Jacobian, or terminal correction, and there is no getattr fallback (getattr on dp targets only Rs_junction L212 and spatial_ L3116). The only Rs applied as terminal IR drop in calc_iv is the legacy lumped path: L5643 Rs_lumped_total = dp.Rs_lumped_top + dp.Rs_lumped_bot, and L5650-5651 _V_terminal subtracts J*1e-3*Rs_lumped_total. Those lumped aliases are themselves marked deprecated (L1037) and also default to 0. So the documented per-subcell vertical transport resistance is silently absent and, defaulting to 0 and never read, cannot even be enabled. Claim accurate. Severity downgraded to medium: this is a dead/unwired parameter (documentation vs code mismatch) not a wrong computation in active code; the wired lumped path is dimensionally correct and lateral grid/TCO/contact R are handled by the FEM grid per the docstring. The missing term overstates FF when vertical transport R is significant.'}] sev->None
   LOC: DiodeParams Rs_internal_top/Rs_internal_bot L1035-1036 (definition + docstring L1026-1034); no use anywhere

[MEDIUM|conf=high] TANDEM-3: Default DiodeParams produce a physically non-credible tandem (Voc=2.27V, Eff~39.7%)  >>VERIFY[{'finding_id': 'TANDEM-3', 'verdict': 'confirmed', 'reasoning': 'All quoted code exists verbatim. L1018: J01_top_pass = 8.8e-26. L1180-1182: expected_voc calls _solve_voc(self.Jph_top, self.J01_top_pass, self.J02_top_pass, self.Rsh_top, self.n1_top, self.n2_top). L2701: Vt_oc = dp.n1_top * VT * np.log(...). VT defined at L108 = kB*T/q_e = 0.025702 V.\n\nI ran the file\'s own math with the shipped class defaults (Jph_top=19.65e-3, J01_top_pass=8.8e-26, J02_top_pass=2.62e-16, n1_top=1.0, n2_top=2.0, Rsh_top=5550; Jph_bot=19.65e-3, J01_bot=1.3339e-17, etc.):\n- expected_voc(): Voc_top=1.3813 V, Voc_bot=0.8976 V, sum=2.2789 V (claim said 1.380/0.897/2.277 — match).\n- solve_0d_tandem_iv() (L2662): Jsc=19.650, Pmpp=39.692 mW/cm2, Eff=39.692%, Voc=2.2727, FF=88.9% (claim said Jsc=19.65, Pmpp=39.66, Eff~39.7% — match).\n\nPhysics verified: at V=1.3813 the J01 recomb term J01*(e^(V/n1*VT)-1)=0.01927 is ~157x the J02 term J02*(e^(V/n2*VT)-1)=0.000123, so the J02 term at L1155 indeed cannot pull Voc down — confirming "J01 dominates near Voc." A strong real ~1.68eV PST top cell (Voc 1.15-1.25 V) needs J01 ~7.3e-22 to ~1.5e-23, i.e. 3-4 orders of magnitude higher than 8.8e-26, so the default top Voc is ~150-230 mV above real devices and the ~39.7% tandem exceeds the ~34% record. The bottom Si Voc (0.898 V) is also optimistic, but the top J01 is the dominant driver as claimed.\n\nNo compensation elsewhere: calc_iv L5637-5642 instantiates DiodeParams() when dp=None and feeds expected_voc()\'s Voc_est=2.279V straight into the sweep range; DP=DiodeParams() at L6283 is the live module-level default. Nothing downstream corrects the optimistic J01. The Voc_top+Voc_bot series summation itself is correct physics; the defect is the unrealistic default top J01.', 'corrected_severity': 'medium', 'corrected_location': 'DiodeParams.J01_top_pass = 8.8e-26 at L1018; consumed by expected_voc (L1180-1182) and solve_0d_tandem_iv (L2697/L2701); default flows live via DiodeParams() at L5638 (calc_iv) and DP=DiodeParams() at L6283'}] sev->None
   LOC: DiodeParams L1017-1024 (J01_top_pass=8.8e-26) consumed by expected_voc L1180 and solve_0d_tandem_iv L2701

[MEDIUM|conf=high] TANDEM-4: Dead v29 Schur solver applies a different (shading-ignoring) bifacial-gain formula  >>VERIFY[{'finding_id': 'TANDEM-4', 'verdict': 'confirmed', 'reasoning': 'All three factual pillars of the claim hold against the actual code. (a) Quoted code is verbatim: L4572 in _solve_tandem_junction_bf_v29 reads `Jph_b_eff = dp.Jph_bot * (1 + dp.bifacial_gain)`; the active solvers at L4280 (_solve_tandem_junction_bf, the 6-plane Phase-B solver) and L4853 (_solve_tandem_bifacial) read `Jph_b_eff = (ilf + dp.bifacial_gain * self.rear_illum_frac) * dp.Jph_bot`. (b) The physics divergence is real and uncompensated: L2972 `self.illum_frac = 1.0 - self.metal_frac` is front-metal shading (per-node), and L2987 `self.rear_illum_frac = 1.0 - self.rear_metal_frac` is rear-metal shading. The v29 formula drops the `ilf` multiplier (so the bottom subcell is illuminated even under front fingers) and replaces `gain*rear_illum_frac` with a bare scalar `gain` (ignoring rear-metal shading). The v29 Jph_b_eff is consumed at L4604 and L4635 inside its Newton loop, so it would corrupt results if executed. The same correct formula recurs at L5191, L5382, L6134, reinforcing that v29 is the odd one out. (c) Dead-code status confirmed: the dispatcher solve_tandem routes every tandem-bifacial path (L3522, L3529, L3540) to _solve_tandem_junction_bf, full_area to _solve_tandem_junction (L3543) and bifacial to _solve_tandem_bifacial (L3548); grep shows the only references to `_solve_tandem_junction_bf_v29` and `_use_v29_constraint_solver` are the method def (L4490) and its own docstring (L4510) — no call site, and the L3531-3534 dispatch comment explicitly states the v29 Schur solver is "kept for future, currently sub-optimal." So the docstring activation flag at L4510 is never honored. The claim is accurate. I only adjust severity: since the routine cannot execute on any code path and the author has explicitly marked it not-for-use, the live impact is zero and the issue is a latent maintainability/correctness trap rather than a medium-severity defect. The reviewer themselves states it "does not corrupt results today." Downgrading medium to minor.', 'corrected_severity': 'minor', 'corrected_location': '_solve_tandem_junction_bf_v29 L4572 (dead); contrast active L4280 (_solve_tandem_junction_bf) and L4853 (_solve_tandem_bifacial); dispatch in solve_tandem L3522/L3529/L3540 never selects v29'}] sev->None
   LOC: _solve_tandem_junction_bf_v29 L4572 vs active solvers L4280/L4853

[MEDIUM|conf=medium] TANDEM-5: No spectral split of top/bottom photocurrent; both default to identical Jph  >>VERIFY[{'finding_id': 'TANDEM-5', 'verdict': 'partially_confirmed', 'reasoning': 'Quoted code is accurate. L1017 `Jph_top = 19.65e-3 # A/cm2` and L1046 `Jph_bot = 19.65e-3` both exist verbatim as class defaults in DiodeParams. The current_matching_diagnostics block exists: L5548 `mismatch = Jph_top_eff - Jph_bot_eff` and L5550-5551 `if abs(mismatch_pct) < 0.5: limiting = \'Current-matched\'`. (There is also a near-duplicate block at L5409-5414.)\n\nThe factual modeling observations are CONFIRMED: (a) both subcells default to the same 19.65 mA/cm2; (b) a grep across the whole file for EQE|spectral|bandgap|spectrum|transmission|AM1.5 returns NO optical-generation model — Jph enters everywhere as a flat constant scaled only by geometric/illumination factors (ilf, _gen, bifacial_gain, spatial_gen); there is no 1.68eV/1.12eV partition of AM1.5G; (c) front-grid shading is applied to BOTH subcells via the same ilf (L5543-5544: `Jph_top_node = ilf*dp.Jph_top`, `Jph_bot_node = ilf*dp.Jph_bot + ...`), which is actually physically correct for a 2T stack since the bottom sits under the top grid; (d) with identical Jph and monofacial illumination, mismatch=0 so the diagnostic reports \'Current-matched\' at defaults.\n\nCRITICAL MITIGATION the reviewer asked to verify: Jph_top and Jph_bot are NOT hardcoded — they are independent, separately-parsed, separately-validated GUI inputs. L7347 ("Jph Top") and L7361 ("Jph Bot") are distinct entry cards; L9100-9107 parse/validate/apply `DP.Jph_top = jph_top` and L9180-9191 apply `DP.Jph_bot = jph_bot` independently. So users CAN and are expected to enter distinct top/bottom photocurrents from an external optical model. The 19.65 values are defaults, not constants. This directly satisfies the reviewer\'s own verification request.\n\nSo the description is accurate as a SCOPE characterization (this is a distributed 2-diode FEM electrical/network solver, like Griddler, with no bundled optical/spectral front-end), but it is NOT a code bug: the framework fully supports the top/bottom Jph asymmetry via independent inputs, and the diagnostic behaves correctly given its inputs. The IMPACT framing (\'artificially perfectly current-matched\', \'bypasses the most important tradeoff\') overstates it — the tool delegates spectral splitting to the user/external optical model rather than getting it wrong.', 'corrected_severity': 'minor', 'corrected_location': 'DiodeParams defaults L1017 (Jph_top=19.65e-3), L1046 (Jph_bot=19.65e-3); current_matching_diagnostics L5543-5555 (and near-duplicate L5409-5414); user overrides at GUI inputs L7347/L7361 and apply sites L9107/L9191'}] sev->None
   LOC: DiodeParams L1017 (Jph_top=19.65e-3) and L1046 (Jph_bot=19.65e-3); current_matching_diagnostics L5543-5555

[MINOR|conf=medium] TANDEM-6: expected_voc / 0D Voc uses pass-only J0 while FEM uses area-weighted pass/metal mix (seed/sweep-range inconsistency)
   LOC: expected_voc L1180-1185 and subcell_iv L1188-1199 (pass-only) vs FEM J01_top_arr L3581 and solve_0d_tandem_iv j_match L2692

[TRUSTWORTHY|conf=high] TANDEM-7: Correct 2T series coupling: node-wise current matching + voltage addition in the FEM solve
   LOC: solve_tandem L3622, L3725, L3794-3796 (full_area) and _solve_tandem_bifacial L4841, L4922

[TRUSTWORTHY|conf=high] TANDEM-8: Luminescent coupling and interlayer-Rc Jacobian are implemented with correct signs and self-consistency
   LOC: solve_tandem LC L3696-3705 and Rc inner Newton L3643-3690; bifacial L4894-4904

####################################################################################################
### DIM: numerics | findings:10 verified:5
SUMMARY: The Newton solvers are reasonably engineered: exp() arguments are uniformly clamped to +80, step sizes are capped, an inner Newton resolves the implicit Rc_junction coupling, and the diode-diagonal-only Jacobian update reuses a pre-assembled static block so the per-iteration cost is sparse and near-O(nnz). However the numerical-stability story has real holes. The dominant scaling bottleneck is NOT the solve but the per-build Python for-loops that re-assemble the static Jacobians node-by-node (L3318-3393, L3408-3478) and the rear assemble_K_met absorption loop (L1964-1989); both are O(Nm) to O(N) Python and dominate wall-time on large-area/dense meshes, and the build runs once per geometry. The deliberate SHORT=1e4..1e3*gc_max equipotential bond combined with the rc>=1e-12 clamp can drive the front-metal sub-block condition number toward 1e16, and spsolve has no singularity/NaN guard, so a 'perfect contact' input can silently return garbage rather than erroring. There is also a latent VT inconsistency (0.02568 vs module 0.025693) and an exp-clamp that is one-sided. The mesh-convergence claim for the 1D metal model is plausible and partially evidenced, but the rear plane and the contact/illumination terms are NOT shown to be mesh-convergent, so the global 'converged' claim is unverified.

[MEDIUM|conf=high] NUM-1: Static-Jacobian assembly is O(N) Python per build → real large-area bottleneck  >>VERIFY[{'finding_id': 'NUM-1', 'verdict': 'confirmed', 'reasoning': 'The quoted code exists verbatim. Gc coupling loop allocating a 4-element np.array per non-zero node: L3318-3324 (`for gi in gc_nz: ... rows.append(np.array([oVe+gi, oVe+gi, oVm+k, oVm+k], dtype=np.int64))`). Km row loop allocating a 1-element np.array per non-zero: L3327-3331 (`for k, gi in enumerate(self.midx): ids, dat = self._Km_rows[k]; for ci, v in zip(ids, dat): if self.ism[ci]: rows.append(np.array([oVm+k])); ...`). These exact patterns repeat across all four blocks: tandem (L3318-3331), tandem-bf (L3355-3393, plus the rear Krm loop L3387-3393), single (L3408-3420), single-bf (L3440-3478, plus rear Krm L3472-3478). All four static Jacobians are assembled unconditionally inside _build (bf variants gated only on rear_mode at L3343/L3429), so a tandem full-area solve still pays to build the single-cell Jacobian and vice-versa — the duplication claim holds.\n\nThe loops are genuinely pure-Python and scale linearly in node count: self.Nm = len(self.midx) (L2927) and self.Nrm = len(self.rear_midx) (L2939) are mesh-node quantities, gc_nz = np.where(self._Gc > 0)[0] (L3317) is ~Nm long. _build is invoked at the top of every solve_tandem (L3498) and solve_single (L5024). The cache hash at L3165-3168 excludes Vb (it keys on rm,hf,wf,wb,rc,Rs_front,cf,rear sheet Rs, Rs_j, spatial tags), so across a fixed-param bias sweep it builds once — exactly as the reviewer acknowledged. The default mesh target is target_nodes=30000 (L1579), confirming dense meshes are normal; Nm in the low-to-mid 1e4 range is realistic on M10/large-area cells, so the per-build cost is real. (1e5 is the optimistic end of their range but not load-bearing.) One detail the reviewer under-stated rather than over-stated: the Rs_junction>50 continuation ramp rebuilds per step (L3519-3521, Rs_j is in the hash), so the build cost can recur multiple times in that path.\n\nEmpirical check: a representative per-nonzero np.array-append loop at Nm=50k costs ~0.35s per block and ~1.4s for all four; ~0.3s total at Nm=1e4. This is purely a performance characterization — the reviewer explicitly and correctly notes it does NOT distort efficiency/FF numerics. It is trivially vectorizable (the Ke/Kr blocks already use tocoo()+concatenate; the Gc/Km/Krm blocks could too). Confirmed as a legitimate, amortized (once-per-geometry, not per-iteration) inefficiency, which is correctly rated medium — not a correctness bug, but a real large-area runtime contributor to the documented ~30s calc_iv budget (docstring L5634-5636).', 'corrected_severity': 'medium', 'corrected_location': '_build L3303-3484; specifically Gc loops L3318-3324/L3355-3361/L3408-3414/L3440-3446, Km row loops L3327-3331/L3364-3370/L3416-3420/L3449-3455, Krm row loops L3387-3393/L3472-3478; cache hash L3165-3168; call sites L3498 (solve_tandem) and L5024 (solve_single); extra rebuild path L3519-3521 (Rs_junction ramp)'}] sev->None
   LOC: _build L3303-3484 (esp. L3318-3331 Gc loop, L3327-3331/3364-3370/3387-3393 Km/Krm row loops, L3408-3478 single+bifacial blocks)

[MEDIUM|conf=medium] NUM-2: SHORT bond (1e4..1e3*gc_max) + rc clamp 1e-12 can make the metal/contact block condition number ~1e16; spsolve has no singularity guard  >>VERIFY[{'finding_id': 'NUM-2', 'verdict': 'partially_confirmed', 'reasoning': 'All quoted code verified exactly. L2036 `SHORT = max(1.0e4, 1.0e3 * float(gc_max))`; L2072-2075 the four bond updates (`K[rep,rep]+=SHORT; K[m,m]+=SHORT; K[rep,m]-=SHORT; K[m,rep]-=SHORT`); L2083 longitudinal `g = 1.0/(R_per_len*dl)`; L3142 `rc = max(float(rc), 1e-12)`; L3234 `_gc_peak = float(np.max(self._na[self.ism]*self.metal_frac[self.ism]))/rc`. Comment L3139-3141 confirms rc=0 is a valid GUI input ("이상 접촉"=ideal contact), and comment L2030-2035 explicitly admits "rc→0 에서는 행렬이 stiff 해지므로 일상 실행은 rc>=1e-4 권장" (at rc→0 the matrix is stiff; recommend rc>=1e-4). ALL spsolve sites confirmed with NO try/except, NO residual check on the linear solve, NO isfinite gate on dV: L3784, L4078, L4417, L4617, L4721, L4974, L5124, L5243.\n\nThe numeric/unit reasoning is correct. `_na` is nodal area in cm² (assemble_K L1888: A/3.0, A=wafer area), `metal_frac` dimensionless 0-1. With rc=1e-12, na~0.01-0.1 cm², mf~1 → _gc_peak~1e10-1e11 → SHORT~1e13-1e14. Longitudinal g=1/(R_per_len*dl)~1e0-1e3; emitter Ke entries 1/(4*A*Rs)~1e2. So the assembled Jacobian genuinely spans ~1e13 (bonds) down to ~1e0 (real finger R), cond(J) approaching ~1e13-1e16 at rc→0 — the code\'s own comment confirms this ill-conditioning. So the SHORT/Gc magnitude chain and the "no spsolve singularity guard / no dV isfinite gate" claim are TRUE.\n\nBUT the most alarming part of the IMPACT ("silent NaNs reach Pmpp, Eff silently wrong") is substantially mitigated by defenses the reviewer omitted: (1) a full-NaN solve makes recomputed residual r=np.max(np.abs(F))=NaN, and solve() L5312 `failed=(not np.isfinite(final_residual)) or final_residual>1e-5` detects it (though it only auto-reruns when used_prolongation, L5313; otherwise just records stats and still returns). (2) calc_iv filters non-finite: L5896 `valid=(Js>0)&(Vs>0)` (NaN→False), L5668 finite filter, L5911 isfinite gate in _power_at_internal, L5684/L5695 bisection finite gates. (3) losses() L6026-6027 uses np.nan_to_num. So a catastrophic NaN solve degrades to Pmpp=0, not a silently-plausible garbage Eff. The genuinely un-mitigated risk is the subtler one: at rc→0, cond(J)~1e13-1e16 erodes the ~7 digits the 1e-10/1e-7 residual tolerance assumes, yielding a finite-but-contaminated V that passes the `<1e-5` gate and silently distorts Rs loss/FF/Eff, with no per-solve condition warning and no dV finite-guard, while rc=0 remains a selectable input against the code\'s own advice. That core problem is real, so the finding holds — but the "NaN reaches Eff silently" worst case is largely refuted by the finite-filtering.', 'corrected_severity': 'medium', 'corrected_location': 'assemble_K_met_1d L2036 (SHORT), L2072-2075 bonds, L2083 longitudinal g; _build L3142 (rc clamp), L3234 (_gc_peak); spsolve with no guard at L3784, L4078, L4417, L4617, L4721, L4974, L5124, L5243; residual-finite check at solve() L5312 (only auto-reruns under prolongation); NaN filtering in calc_iv L5668/L5896/L5911 and losses L6026-6027'}] sev->None
   LOC: assemble_K_met_1d L2036 (SHORT), L2072-2075 bonds; _build L3142 (rc clamp), L3234 (_gc_peak); spsolve L3784, L4078, L4617, L4974, L5124

[MEDIUM|conf=medium] NUM-3: One-sided exp() clamp caps the diode value but not its analytic Jacobian → inconsistent F vs dF near forward bias  >>VERIFY[{'finding_id': 'NUM-3', 'verdict': 'partially_confirmed', 'reasoning': 'The quoted code exists verbatim. solve_tandem L3625-3630 uses `e1t = np.exp(np.minimum(Vtop/(dp.n1_top*VT), 80))` and computes both `Jt` (L3628) and `dJt` (L3629-3630) from that SAME clamped `e1t`. Same pattern at L3675-3680 (bottom), _solve_tandem_junction L3909-3924, and solve_single L5078-5083. So the reviewer\'s CORE mechanism claim is factually true: in the clamped region `d(e1t)/dVtop` is actually 0 (the np.minimum freezes the argument) yet `dJt` still reports `e1t/(n*VT) != 0`, so the analytic Jacobian is not the true derivative of the (frozen) residual there. That local F/dF inconsistency is real.\n\nHowever the IMPACT is materially overstated and the trigger region is essentially unphysical/unreachable, so the severity is wrong:\n\n1. Clamp threshold is far outside any operating point. VT = kB*T/q ≈ 0.0257 V (L108/L448). With n1_top=1.0 (L1022), arg=80 is reached only at Vtop = 80*1.0*0.0257 ≈ 2.06 V; the n2 channel (n2_top=2.0) clamps at ≈ 4.1 V. Perovskite top-subcell diode voltage near tandem Voc is at most ~1.2 V; Si/single diode voltage ~0.75 V. The reviewer themselves concede the cap is "far beyond any physical operating point (~30*n*VT)" i.e. ~0.77 V.\n\n2. The seeding + caps keep Vtop bounded below the clamp. Vtop is seeded at Vb*frac*0.95 (L3559) with frac≈0.4-0.5, i.e. < Vb. Newton dV is capped at 0.3 V/iter (L3786-3787), and after every step Vtop is clipped to [-0.5, Ve-Vr+1.5] (L3794-3796). Any transient overshoot above 2.06 V is pulled back within one iteration by that clip, so the clamp region is not a stable iterate.\n\n3. Magnitude reasoning in the claim is off. The reviewer used "J01~1e-13"; actual J01_top_pass = 8.8e-26 (L1018), J02_top_pass = 2.62e-16 (L1019). The "e^80 * J01" blow-up is for the n1 channel that only clamps at 2.06 V (unreachable), while the n2 channel that would clamp lower in arg-space actually needs ~4.1 V (above the clip ceiling), so it never clamps. For single mode (Vd = Ve - Vr, Ve≈Vb*0.95, Vb≤~0.75 V) the clamp is wholly unreachable.\n\n4. The "r<1e-10 can never be met → forced to stagnation-escape/250-iter cap" outcome is speculative: a clamped F entry would persist for at most one iteration before the L3794 clip restores Vtop, and the stagnation-escape (L3804-3807) only fires when the residual is BOTH flat over 6 iters AND already below 1e-6 — a benign acceptance, not a corruption.\n\nThe clamp is a deliberate, standard overflow guard (without it np.exp overflows to inf/NaN). The F/dF inconsistency it introduces is confined to an unphysical, transient, single-iteration region that the existing dV cap and Vtop clip already neutralize. This is a real but minor numerical nuance, not a medium-severity convergence/accuracy defect affecting Voc/FF.', 'corrected_severity': 'minor', 'corrected_location': 'solve_tandem L3625-3630 (top), L3675-3680 (bottom); _solve_tandem_junction L3909-3924; solve_single L5078-5083; clamp threshold context: VT def L108/L448, n1_top L1022, J01_top_pass L1018; bounding via dV cap L3786-3787 and Vtop clip L3794-3796'}] sev->None
   LOC: all solver loops, e.g. solve_tandem L3625-3630/L3675-3680; _solve_tandem_junction L3909-3924; solve_single L5078-5083

[MINOR|conf=high] NUM-4: Dead v29 Schur solver hard-codes VT=0.02568, inconsistent with module VT=0.025693 (different temperature)
   LOC: _solve_tandem_junction_bf_v29 L4571 vs module VT L448

[MEDIUM|conf=high] NUM-5: Rear assemble_K_met still uses pure-Python lil row absorption loop → O(N) Python with set membership tests on every bifacial build  >>VERIFY[{'finding_id': 'NUM-5', 'verdict': 'partially_confirmed', 'reasoning': 'The quoted code exists verbatim. L1960-1991 of assemble_K_met: K_lil = K.tolil(); nonmetal_idx = np.where(~ism)[0]; nonmetal_set = set(nonmetal_idx.tolist()); metal_idx = np.where(ism)[0]; the `for i in metal_idx:` loop with `if c_idx in nonmetal_set: diag_add += val`; and the second `for i in nonmetal_idx: K_lil.rows[i] = []; K_lil.data[i] = []`. And L3193-3199 confirms the rear bifacial/patterned plane calls assemble_K_met for self._Krm. So the structural facts (rear uses the 2D loop assembler; there is an O(N) Python row-blanking loop) are TRUE.\n\nHowever the claim is materially overstated/misframed on three counts:\n\n(1) FALSE PREMISE that the front is "vectorized" while only the rear is "pure-Python loop." The front now calls assemble_K_met_1d (L3246), but that function (L1994-2087) is ALSO a pure-Python lil-based assembler: nested Python loops over line_centers, groups, reps, and longitudinal segments, with scalar `K[i,i] += g` LIL writes. It is NOT vectorized. So the asymmetric "rear is the slow Python path, front is fast" contrast that motivates the finding is inaccurate — both planes use Python-loop assemblers with comparable per-node overhead.\n\n(2) NOT per-solve. _build is cached: L3166-3167 `if self._cache_hash == h: return`. The static Jacobians are pre-assembled once (L3303 "done ONCE"); per-iteration solve only updates the diode diagonal. So the O(N) loop runs once per geometry/parameter change, not "per build" in any hot loop — except the high-Rs_junction homotopy ramp (L3519-3521) which deliberately invalidates the cache per ramp step (special case, Rs_j>50 only). The "tens of millions of Python ops per build... build-time blowup" framing implies a hot-path cost it does not have; the blanking loop body is two attribute writes per node, a sub-second one-time cost even at N=138000 (Max mesh, L1214).\n\n(3) The physics concern (diagonal absorption changes the operator; mesh-convergence not demonstrated for the rear; "materially moves efficiency") is plausible but UNVERIFIED speculation. The docstring (L1905-1909) gives a defensible rationale (preserve metal-subspace row-sum-zero / Galerkin invariance under V->V+const). The 38%->24% anecdote is about the connectivity fix (2/3-metal element inclusion), not evidence that the rear specifically has residual mesh-dependence perturbing reported FF/Eff. No demonstration of an actual wrong number is given.\n\nNet: a genuine but minor performance/consistency observation (the rear still uses the 2D sheet model the front abandoned, with an O(N) one-time Python loop), not a correctness bug and not a build-time "blowup." Severity should drop from medium to minor.', 'corrected_severity': 'minor', 'corrected_location': 'assemble_K_met L1960-1991 (rear caller at _build L3193-3199); note front 1D assembler assemble_K_met_1d L1994-2087 is also a Python-loop (not vectorized); build is cached at L3166-3167'}] sev->None
   LOC: assemble_K_met L1960-1991 (called for rear at _build L3193)

[MEDIUM|conf=medium] NUM-6: Global mesh-convergence claim is only substantiated for the front 1D metal model; rear/contact/illumination convergence is unverified  >>VERIFY[{'finding_id': 'NUM-6', 'verdict': 'partially_confirmed', 'reasoning': 'All factual code citations are accurate. (1) The docstring at L1994-2016 exists verbatim, including the quoted "Verified: 50 um finger, rf 2->10, efficiency spread 0.59% (2D) -> 0.08% (1D); ... matches it to 0.05% abs" (L2003-2005) and the closing "Mesh-independent regardless of line width, because resistance depends only on node SPACING, not element shape" (L2015-2016). That evidence is indeed scoped to FRONT metal longitudinal finger conductance, not global efficiency. (2) The seeding caps exist verbatim: across_x_segments = min(4, 2 << tang_power) / across_y_segments = min(4, 2 << perp_power) (L1491-1492), with the L1488-1490 comment confirming transverse metal-strip layers are deliberately frozen ("over-refining those strips makes the FEM system ill-conditioned without improving the area-integrated result"). (3) mesh_quality_metrics (L1264-1307) computes MaxAspect/MinAngle_deg/P05Angle_deg but enforces NO bound; grep shows its only consumer is L8298, a GUI mesh-convergence study that merely writes those metrics + dEff/dFF into a CSV report (L8308-8353). There is no feedback into mesh generation or the solve. (4) Contact term Gc = na*metal_frac/rc (L3252) and illum_frac = 1 - metal_frac (L2972) are exactly the area-weighted lumped quantities described; na comes from assemble_K nodal areas (L1886-1888) which redistribute with the mesh; rear analogues exist (_Gc_rear L3204-3205, _Kr/_Krm L3187-3199). So claims (a) and (b) are factually correct: only the 1D metal model\'s convergence is substantiated; emitter/rear/contact/illumination convergence is asserted but not demonstrated in the cited evidence.\n\nHowever the claim is OVERSTATED as a defect: (i) the assemble_K_met_1d docstring itself does not make a blanket GLOBAL mesh-convergence claim — it is scoped to the 1D metal model; the generalizing language ("efficiency to diverge under refinement"/"The 1D model is convergent") lives in the _build comments at L3221-3225 and motivates the metal fix specifically. (ii) The codebase actually SHIPS the verification mechanism the reviewer says is missing: the GUI mesh-convergence study (L8285-8353) sweeps named levels and reports per-level dEff/dFF/dVoc/dJsc plus MaxAspect/P05Angle, so global convergence is empirically checkable by a user. (iii) There is no wrong unit, wrong formula, or numerical error here; the emitter/rear/contact/illumination terms are standard FEM area-weighting, and the min(4,...) cap is on narrow metal-strip transverse layers only, with a documented claim (not refuted by the evidence shown) that it does not change the area-integrated result. This is a documentation-scope / verification-gap concern, not a behavioral bug, and the impact ("a user accepts a non-converged value") is mitigated by the existing convergence tool. Hence partially_confirmed and severity reduced from medium to minor.', 'corrected_severity': 'minor', 'corrected_location': 'Generalizing convergence language is in _build at L3221-3225 (not the docstring); the cited 1D evidence is in the assemble_K_met_1d docstring L2003-2005,2015-2016; metal-strip transverse cap at L1491-1492; mesh_quality_metrics computed-but-unenforced at L1264-1307 with sole consumer the GUI study at L8298; contact term L3252 and illumination term L2972 (rear analogues L3187-3205). Existing convergence-study tool that mitigates: L8285-8353.'}] sev->None
   LOC: assemble_K_met_1d docstring L1995-2016 (claim); generate_mesh feature seeding L1488-1495; _build rear path L3181-3211

[MINOR|conf=medium] NUM-7: Levenberg-Marquardt damping is ad hoc and is added to the BC identity rows, slightly violating Dirichlet conditions when it activates
   LOC: _solve_tandem_junction L4062-4075 (interaction with BC identity at L4055)

[MINOR|conf=high] NUM-8: Per-iteration Jacobian uses many full csr_matrix additions / full COO re-assembly instead of in-place data updates
   LOC: solve_tandem L3747-3781; solve_single L5108-5122; _solve_tandem_junction L3963-4056

[TRUSTWORTHY|conf=high] NUM-9: Loss terms evaluated as V^T K V quadratic forms on the assembled operators — robust against near-equal-potential cancellation
   LOC: losses L6028-6063 (Pe, P_rear, metal edge loss)

[TRUSTWORTHY|conf=high] NUM-10: exp argument clamp + Vtop clip + dV step cap together prevent overflow — the overflow-avoidance design is correct
   LOC: solve_tandem L3625-3626, L3786-3796; _solve_tandem_junction L3909-3919, L4088-4099